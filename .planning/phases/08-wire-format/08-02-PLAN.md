---
phase: 08-wire-format
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - rust/src/api/types.rs
  - .planning/phases/08-wire-format/WIRE-FORMAT.md
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "DKG 4 轮 + commitment_2 交换步骤的 JSON 信封格式已文档化"
    - "DSG 4 轮 + partial signature + combine 的 JSON 信封格式已文档化"
    - "Rotation 复用 DKG 路径的说明已文档化"
    - "WireEnvelope Rust struct 可序列化/反序列化为 JSON 往返"
    - "payload_encoding 支持 cbor_base64 默认值"
  artifacts:
    - path: ".planning/phases/08-wire-format/WIRE-FORMAT.md"
      provides: "冻结的 wire format 规范文档"
      contains: "Wire Format"
    - path: "rust/src/api/types.rs"
      provides: "WireEnvelope struct"
      contains: "pub struct WireEnvelope"
  key_links:
    - from: "rust/src/api/types.rs"
      to: ".planning/phases/08-wire-format/WIRE-FORMAT.md"
      via: "WireEnvelope struct 与文档规范一致"
      pattern: "WireEnvelope"
---

<objective>
定义 DKG/DSG/Rotation 各轮的 JSON wire format 信封结构，创建 WireEnvelope Rust 类型和冻结规范文档。

Purpose: 满足 INFRA-04 — 后续 Phase 9-11 可直接引用 wire format 结构进行协议消息传输。
Output: types.rs 中的 WireEnvelope/ProtocolType 类型 + WIRE-FORMAT.md 冻结规范 + 序列化测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wire-format/08-RESEARCH.md
@.planning/phases/08-wire-format/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01 完成后 types.rs 会包含 MessageDigest，此 plan 在其下方添加 WireEnvelope -->

From rust/src/api/types.rs (existing + Plan 01 additions):
```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MpcRoundResult {
    pub status: String,
    pub round: i32,
    pub client_payload: Option<String>,
    pub error_message: Option<String>,
}

// ... 其他现有类型 ...

// Plan 01 新增：
pub struct MessageDigest([u8; 32]);
```

From 08-RESEARCH.md (wire format envelope design):
```json
{
  "session_id": "hex-string-32-bytes",
  "protocol": "dkg" | "dsg" | "rotation",
  "round": 1 | 2 | 3 | 4,
  "from_id": 0,
  "to_id": null | 1,
  "payload_encoding": "cbor_base64",
  "payload": "base64-or-hex-encoded-cbor-bytes"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WireEnvelope + ProtocolType Rust 类型 + 序列化测试</name>
  <files>rust/src/api/types.rs</files>
  <read_first>
    - rust/src/api/types.rs
    - .planning/phases/08-wire-format/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test: WireEnvelope 序列化为 JSON 后反序列化，所有字段一致（往返测试）
    - Test: ProtocolType::Dkg 序列化为 "dkg"（小写）
    - Test: ProtocolType::Dsg 序列化为 "dsg"（小写）
    - Test: ProtocolType::Rotation 序列化为 "rotation"（小写）
    - Test: to_id 为 None 时 JSON 中 to_id 为 null（broadcast 场景）
    - Test: to_id 为 Some(1) 时 JSON 中 to_id 为 1（P2P 场景）
    - Test: payload_encoding 默认值为 "cbor_base64"
  </behavior>
  <action>
在 rust/src/api/types.rs 中（MessageDigest 定义之后）添加：

```rust
/// 协议类型枚举，用于 wire format 信封路由。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum ProtocolType {
    Dkg,
    Dsg,
    Rotation,
}

/// client ↔ server 协议消息的统一 JSON 信封。
/// dkls23-ll 消息通过 serde 序列化后放入 payload 字段。
/// 此结构在 Phase 8 冻结，Phase 9-13 直接使用。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WireEnvelope {
    /// 32 字节 session ID 的 hex 编码
    pub session_id: String,
    /// 协议类型
    pub protocol: ProtocolType,
    /// 轮次编号 (1-4)
    pub round: u8,
    /// 发送方 party ID
    pub from_id: u8,
    /// 接收方 party ID（None = broadcast）
    pub to_id: Option<u8>,
    /// payload 编码方式（默认 "cbor_base64"）
    pub payload_encoding: String,
    /// 编码后的 dkls23-ll 消息（Base64 编码的 CBOR 字节或 JSON string）
    pub payload: String,
}

impl WireEnvelope {
    /// 构造新信封，payload_encoding 默认为 "cbor_base64"
    pub fn new(
        session_id: String,
        protocol: ProtocolType,
        round: u8,
        from_id: u8,
        to_id: Option<u8>,
        payload: String,
    ) -> Self {
        Self {
            session_id,
            protocol,
            round,
            from_id,
            to_id,
            payload_encoding: "cbor_base64".to_string(),
            payload,
        }
    }
}
```

在 `#[cfg(test)] mod tests` 中添加上述 behavior 对应的测试用例。注意测试应追加到 Plan 01 已创建的 tests 模块中。
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/rust && cargo test wire -- --nocapture 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pub struct WireEnvelope" rust/src/api/types.rs
    - grep -q "pub enum ProtocolType" rust/src/api/types.rs
    - grep -q "Dkg" rust/src/api/types.rs
    - grep -q "Dsg" rust/src/api/types.rs
    - grep -q "Rotation" rust/src/api/types.rs
    - grep -q "cbor_base64" rust/src/api/types.rs
    - grep -q "payload_encoding" rust/src/api/types.rs
    - cargo test wire 全部通过
  </acceptance_criteria>
  <done>WireEnvelope 和 ProtocolType 类型定义完成，JSON 序列化/反序列化往返测试通过，broadcast (to_id=null) 和 P2P (to_id=N) 场景覆盖</done>
</task>

<task type="auto">
  <name>Task 2: 创建 WIRE-FORMAT.md 冻结规范文档</name>
  <files>.planning/phases/08-wire-format/WIRE-FORMAT.md</files>
  <read_first>
    - .planning/phases/08-wire-format/08-RESEARCH.md
    - rust/src/api/types.rs
  </read_first>
  <action>
创建 `.planning/phases/08-wire-format/WIRE-FORMAT.md`，内容为 DKG/DSG/Rotation 各轮的完整 wire format 规范。

文档必须包含以下章节：

**1. 统一信封格式** — WireEnvelope JSON schema，字段说明，payload_encoding 说明

**2. DKG (Keygen) 4 轮流程：**
- Round 1: KeygenMsg1（broadcast），from_id=设备方, to_id=null
- Round 2: KeygenMsg2（P2P），from_id/to_id 指定
- Round 3a: commitment_2 交换（broadcast）— 重要：这是 dkls23-ll 特有的额外步骤，在 handle_msg2 后、handle_msg3 前需要广播 commitment_2
- Round 3b: KeygenMsg3（P2P）
- Round 4: KeygenMsg4（broadcast）
- 完成：handle_msg4 → Keyshare

**3. DSG (Signing) 4 轮流程：**
- Round 1: SignMsg1（broadcast）
- Round 2: SignMsg2（P2P）
- Round 3: SignMsg3（P2P）
- 完成 PreSignature → create_partial_signature(MessageDigest) → SignMsg4（broadcast）
- combine_signatures → Signature

**4. Rotation 流程：**
- 说明 Rotation 复用 DKG 路径（State::key_rotation → handle_msg1/2/3/4）
- finish_key_rotation 额外步骤

**5. 消息路由类型表：**
每条消息的 protocol、round、broadcast/P2P 标记

**6. 冻结声明：**
"本文档在 Phase 8 冻结。Phase 9-13 实现时应直接引用此规范，不应修改信封格式。"

每个轮次包含一个 JSON 示例（使用 WireEnvelope 格式）。

消息类型字段参考 08-RESEARCH.md 中的详细表格（KeygenMsg1-4, SignMsg1-4 字段），但文档中明确声明 payload 内容为不透明字节串，由 dkls23-ll serde 序列化产出，不需要手动解析。
  </action>
  <verify>
    <automated>test -f /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/.planning/phases/08-wire-format/WIRE-FORMAT.md && grep -q "Wire Format" /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/.planning/phases/08-wire-format/WIRE-FORMAT.md && grep -q "commitment_2" /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/.planning/phases/08-wire-format/WIRE-FORMAT.md && grep -q "冻结" /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/.planning/phases/08-wire-format/WIRE-FORMAT.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 文件 .planning/phases/08-wire-format/WIRE-FORMAT.md 存在
    - grep -q "WireEnvelope" .planning/phases/08-wire-format/WIRE-FORMAT.md
    - grep -q "DKG" .planning/phases/08-wire-format/WIRE-FORMAT.md
    - grep -q "DSG" .planning/phases/08-wire-format/WIRE-FORMAT.md
    - grep -q "Rotation" .planning/phases/08-wire-format/WIRE-FORMAT.md
    - grep -q "commitment_2" .planning/phases/08-wire-format/WIRE-FORMAT.md（Round 3a 额外步骤未遗漏）
    - grep -q "冻结" .planning/phases/08-wire-format/WIRE-FORMAT.md（冻结声明存在）
    - grep -q "cbor_base64" .planning/phases/08-wire-format/WIRE-FORMAT.md
    - grep -q "MessageDigest" .planning/phases/08-wire-format/WIRE-FORMAT.md
  </acceptance_criteria>
  <done>WIRE-FORMAT.md 包含完整的 DKG 4+1 轮、DSG 4 轮、Rotation 流程规范，含 JSON 示例和冻结声明，commitment_2 交换步骤已记录</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> client WireEnvelope | server 返回的信封可能包含恶意 payload |
| client -> server WireEnvelope | client 发送的信封格式需严格遵循规范 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | WireEnvelope from_id | accept | from_id 字段验证推迟到 Phase 9-11 协议实现层，Phase 8 仅定义格式 |
| T-08-05 | Tampering | WireEnvelope payload | accept | payload 完整性由 dkls23-ll 协议层保证（密码学绑定），Phase 8 不涉及验证逻辑 |
| T-08-06 | Repudiation | session_id 重放 | accept | session_id 唯一性检查推迟到 Phase 9-11 SessionMap 实现 |
</threat_model>

<verification>
- `cargo test wire` — WireEnvelope 序列化/反序列化往返测试通过
- `cargo test` — 全量测试通过（含 Plan 01 的 MessageDigest 测试）
- WIRE-FORMAT.md 存在且包含 DKG/DSG/Rotation 完整轮次规范
- commitment_2 交换步骤已在文档中记录（防止 Phase 9 实现时遗漏）
</verification>

<success_criteria>
- WireEnvelope struct 定义在 types.rs，含 session_id/protocol/round/from_id/to_id/payload_encoding/payload 字段
- ProtocolType 枚举含 Dkg/Dsg/Rotation，serde 序列化为小写
- JSON 往返测试通过（broadcast 和 P2P 场景）
- WIRE-FORMAT.md 包含完整协议轮次规范和冻结声明
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-format/08-02-SUMMARY.md`
</output>
