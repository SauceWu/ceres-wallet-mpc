---
phase: 08-wire-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/src/api/types.rs
  - rust/src/api/mpc_engine.rs
autonomous: true
requirements:
  - SEC-03

must_haves:
  truths:
    - "MessageDigest newtype 只能从 [u8; 32] 或 hex string 构造"
    - "Vec<u8> 直接传给内部签名函数产生编译错误"
    - "sign_start 接受 message_hash_hex 参数并在 Rust 边界转换为 MessageDigest"
    - "from_hex 对非 32 字节输入返回 Err"
  artifacts:
    - path: "rust/src/api/types.rs"
      provides: "MessageDigest newtype 定义"
      contains: "pub struct MessageDigest"
    - path: "rust/src/api/mpc_engine.rs"
      provides: "sign_start 含 message_hash_hex 参数"
      contains: "message_hash_hex"
  key_links:
    - from: "rust/src/api/mpc_engine.rs"
      to: "rust/src/api/types.rs"
      via: "use crate::api::types::MessageDigest"
      pattern: "MessageDigest::from_hex"
---

<objective>
引入 MessageDigest newtype 安全类型，更新 sign_start 签名，确保签名函数不接受原始 Vec<u8>。

Purpose: 满足 SEC-03 — 编译时防止原始字节误传签名函数。MessageDigest 是后续 Phase 10 DSG 签名的类型基础。
Output: types.rs 中的 MessageDigest 类型 + mpc_engine.rs 中更新的 sign_start 签名 + 单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wire-format/08-RESEARCH.md

<interfaces>
<!-- 现有类型，executor 需要了解 types.rs 的当前结构 -->

From rust/src/api/types.rs:
```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MpcRoundResult {
    pub status: String,
    pub round: i32,
    pub client_payload: Option<String>,
    pub error_message: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SignCompletedPayload {
    pub r: String,
    pub s: String,
    pub recid: u8,
}
```

From rust/src/api/mpc_engine.rs (signing section):
```rust
pub fn sign_start(
    session_id: String,
    share: String,
    server_payload: String,
) -> Result<String, String> {
    Err("not implemented: signing uses dkls23-ll, see Phase 10".to_string())
}
```

From rust/Cargo.toml:
```toml
hex = "0.4"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 MessageDigest newtype + 单元测试</name>
  <files>rust/src/api/types.rs</files>
  <read_first>
    - rust/src/api/types.rs
    - rust/Cargo.toml
  </read_first>
  <behavior>
    - Test: MessageDigest::new([0u8; 32]) 成功构造，into_bytes() 返回相同数组
    - Test: MessageDigest::from_hex("00".repeat(32)) 成功构造
    - Test: MessageDigest::from_hex("00".repeat(31)) 返回 Err（31 字节）
    - Test: MessageDigest::from_hex("00".repeat(33)) 返回 Err（33 字节）
    - Test: MessageDigest::from_hex("not_hex") 返回 Err（无效 hex）
    - Test: MessageDigest::from_hex("") 返回 Err（空字符串）
  </behavior>
  <action>
在 rust/src/api/types.rs 末尾添加 MessageDigest newtype：

```rust
/// 32 字节消息摘要的安全类型包装。
/// 防止将任意 Vec<u8> 直接传入签名函数。
/// 不实现 From<Vec<u8>>、From<&[u8]> 或 From<[u8; 32]>。
#[derive(Debug, Clone, Copy)]
pub struct MessageDigest([u8; 32]);

impl MessageDigest {
    /// 从精确的 32 字节数组构造。
    pub fn new(bytes: [u8; 32]) -> Self {
        Self(bytes)
    }

    /// 从 hex string 构造（FRB Dart 侧传入路径）。
    pub fn from_hex(s: &str) -> Result<Self, String> {
        let bytes = hex::decode(s)
            .map_err(|e| format!("hex decode failed: {e}"))?;
        let arr: [u8; 32] = bytes
            .try_into()
            .map_err(|_| "message digest must be exactly 32 bytes".to_string())?;
        Ok(Self(arr))
    }

    /// 获取底层字节（传给 dkls23-ll create_partial_signature）。
    pub fn into_bytes(self) -> [u8; 32] {
        self.0
    }

    /// 引用底层字节。
    pub fn as_bytes(&self) -> &[u8; 32] {
        &self.0
    }
}
```

关键约束：
- 不实现 `From<Vec<u8>>`
- 不实现 `From<&[u8]>`
- 不实现 `From<[u8; 32]>`
- 只有 `new()` 和 `from_hex()` 两个构造路径

在同一文件底部添加 `#[cfg(test)] mod tests` 模块，包含上述 behavior 中列出的所有测试用例。
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/rust && cargo test message_digest -- --nocapture 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pub struct MessageDigest" rust/src/api/types.rs
    - grep -q "fn from_hex" rust/src/api/types.rs
    - grep -q "fn into_bytes" rust/src/api/types.rs
    - grep -q "fn as_bytes" rust/src/api/types.rs
    - grep -q "exactly 32 bytes" rust/src/api/types.rs
    - NOT: grep -q "impl From<Vec<u8>> for MessageDigest" rust/src/api/types.rs
    - NOT: grep -q "impl From<\[u8" rust/src/api/types.rs
    - cargo test message_digest 全部通过
  </acceptance_criteria>
  <done>MessageDigest newtype 定义完成，6 个单元测试全部通过，不存在 From<Vec<u8>> 或 From<[u8; 32]> 实现</done>
</task>

<task type="auto">
  <name>Task 2: 更新 sign_start 签名 + 内部 sign_with_digest 函数</name>
  <files>rust/src/api/mpc_engine.rs</files>
  <read_first>
    - rust/src/api/mpc_engine.rs
    - rust/src/api/types.rs
  </read_first>
  <action>
修改 rust/src/api/mpc_engine.rs：

1. 在文件顶部 use 语句中添加 `MessageDigest`：
```rust
use crate::api::types::{BackupEnvelope, DecryptBackupResult, MessageDigest};
```

2. 更新 sign_start 函数签名，添加 message_hash_hex 参数，在 Rust 边界立即转为 MessageDigest：
```rust
pub fn sign_start(
    session_id: String,
    share: String,
    message_hash_hex: String,
    server_payload: String,
) -> Result<String, String> {
    // Rust 边界立即转换为 MessageDigest，确保 Vec<u8> 不能直接传入
    let _digest = MessageDigest::from_hex(&message_hash_hex)?;
    Err("not implemented: signing uses dkls23-ll, see Phase 10".to_string())
}
```

3. 在 sign_start 下方添加内部函数（不暴露给 FRB）：
```rust
/// 内部签名逻辑入口 — 类型系统强制只接受 MessageDigest。
/// Phase 10 实现具体 DSG 协议逻辑。
#[allow(dead_code)]
fn sign_with_digest(
    _session_id: &str,
    _digest: MessageDigest,
) -> Result<String, String> {
    Err("not implemented: see Phase 10".to_string())
}
```

此设计确保：
- FRB 公开 API（sign_start）保持 String 参数兼容 Dart
- 内部逻辑函数（sign_with_digest）强制 MessageDigest 类型
- 任何尝试传 Vec<u8> 给 sign_with_digest 的代码产生编译错误
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/rust && cargo check 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "message_hash_hex: String" rust/src/api/mpc_engine.rs
    - grep -q "MessageDigest::from_hex" rust/src/api/mpc_engine.rs
    - grep -q "fn sign_with_digest" rust/src/api/mpc_engine.rs
    - grep -q "use crate::api::types::{.*MessageDigest" rust/src/api/mpc_engine.rs
    - cargo check 无错误通过
  </acceptance_criteria>
  <done>sign_start 新增 message_hash_hex 参数并在 Rust 边界转换为 MessageDigest，sign_with_digest 内部函数只接受 MessageDigest 类型，cargo check 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart (FRB) -> Rust sign_start | 不受信任的 hex string 从 Dart 传入 Rust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | sign_start message_hash_hex | mitigate | MessageDigest::from_hex 验证 hex 合法性 + 长度精确 32 字节，否则返回 Err（V5 输入验证） |
| T-08-02 | Tampering | sign_with_digest | mitigate | Rust 类型系统强制 MessageDigest newtype，Vec<u8> 无法隐式转换（V6 密码学安全） |
| T-08-03 | Tampering | MessageDigest 构造 | mitigate | 不实现 From<Vec<u8>> / From<[u8; 32]> trait，只有显式 new() 和 from_hex() 两个构造路径 |
</threat_model>

<verification>
- `cargo test message_digest` — 所有 MessageDigest 单元测试通过
- `cargo check` — 编译无错误
- sign_start 参数列表含 message_hash_hex
- sign_with_digest 只接受 MessageDigest 类型
</verification>

<success_criteria>
- MessageDigest newtype 在 types.rs 中定义，含 new/from_hex/into_bytes/as_bytes 方法
- 不存在 From<Vec<u8>> 或 From<[u8; 32]> 实现
- sign_start 新增 message_hash_hex 参数，内部调用 MessageDigest::from_hex
- sign_with_digest 内部函数参数为 MessageDigest（非 Vec<u8>）
- 6 个单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-format/08-01-SUMMARY.md`
</output>
