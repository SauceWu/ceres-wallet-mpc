---
phase: 01-rust-bridge-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/Cargo.toml
  - rust/src/lib.rs
  - rust/src/api/mod.rs
  - rust/src/api/types.rs
  - rust/src/api/mpc_engine.rs
  - flutter_rust_bridge.yaml
  - pubspec.yaml
  - lib/flutter_mpc_wallet.dart
autonomous: true
requirements: [MPC-01, MPC-02, MPC-03]

must_haves:
  truths:
    - "rust/ 目录包含可编译的 Rust crate（cargo check 通过）"
    - "FRB codegen 可成功从 Rust 源码生成 Dart 绑定到 lib/src/rust/"
    - "6 个 stub 函数（keygen/recover/sign 各 _start/_continue）在 Rust 侧定义并通过 codegen 映射到 Dart"
    - "Rust DTO 结构体与 doc/architecture/mpc_wallet_integration_plan.md 字段草案对齐"
    - "flutter_mpc_wallet 仍可作为独立 Flutter package 使用（pubspec 保持 package 结构）"
  artifacts:
    - path: "rust/Cargo.toml"
      provides: "Rust crate 配置，依赖 flutter_rust_bridge =2.12.0 + serde"
      contains: "name = \"rust_lib_flutter_mpc_wallet\""
    - path: "rust/src/api/types.rs"
      provides: "MPC 协议 DTO：MpcSessionRequest, MpcRoundResult, KeygenCompleteResult, RecoverCompleteResult, SignCompleteResult"
      exports: ["MpcSessionRequest", "MpcRoundResult", "KeygenCompleteResult", "RecoverCompleteResult", "SignCompleteResult"]
    - path: "rust/src/api/mpc_engine.rs"
      provides: "6 个 stub 函数，返回 Result<T, String> 包装的 mock 数据"
      exports: ["keygen_start", "keygen_continue", "recover_start", "recover_continue", "sign_start", "sign_continue"]
    - path: "flutter_rust_bridge.yaml"
      provides: "FRB codegen 配置，dart_entrypoint_class_name = MpcBridge"
      contains: "dart_entrypoint_class_name: MpcBridge"
    - path: "lib/src/rust/api/mpc_engine.dart"
      provides: "FRB 自动生成的 Dart 绑定（6 个 stub 函数）"
    - path: "lib/src/rust/api/types.dart"
      provides: "FRB 自动生成的 Dart DTO class"
  key_links:
    - from: "rust/src/api/mpc_engine.rs"
      to: "lib/src/rust/api/mpc_engine.dart"
      via: "flutter_rust_bridge_codegen generate"
      pattern: "keygen_start|keygen_continue|recover_start|recover_continue|sign_start|sign_continue"
    - from: "rust/src/api/types.rs"
      to: "lib/src/rust/api/types.dart"
      via: "flutter_rust_bridge_codegen generate"
      pattern: "MpcRoundResult|KeygenCompleteResult"
---

<objective>
建立 flutter_rust_bridge v2 基础设施，创建 Rust crate 和 6 个 MPC stub 函数，运行 codegen 生成 Dart 绑定。

Purpose: 这是整个 MPC 钱包的地基——没有 Rust bridge 骨架，后续所有 Phase 都无法开始（per MPC-02）。
Output: 可编译的 Rust crate + FRB codegen 产出的 Dart 绑定 + 项目配置文件。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-rust-bridge-skeleton/01-CONTEXT.md
@.planning/phases/01-rust-bridge-skeleton/01-RESEARCH.md
@doc/architecture/mpc_wallet_integration_plan.md
@pubspec.yaml
@lib/flutter_mpc_wallet.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: FRB Integrate + 项目基础配置</name>
  <files>pubspec.yaml, flutter_rust_bridge.yaml, rust/Cargo.toml, rust/src/lib.rs, rust/src/api/mod.rs, lib/flutter_mpc_wallet.dart</files>
  <read_first>
    - pubspec.yaml（当前 package 配置，无 Rust 依赖）
    - lib/flutter_mpc_wallet.dart（现有 FlutterMpcWalletPackage marker class，integrate 可能覆盖）
    - .planning/phases/01-rust-bridge-skeleton/01-RESEARCH.md（FRB Integration Steps、Common Pitfalls 章节）
  </read_first>
  <action>
    **Step 1: 安装 CLI 工具**
    ```bash
    cargo install flutter_rust_bridge_codegen
    cargo install cargo-ndk
    ```

    **Step 2: 安全网 — 在 integrate 前保存当前状态**
    ```bash
    git add -A && git commit -m "chore: snapshot before FRB integrate"
    ```

    **Step 3: 运行 FRB integrate（per D-03: Rust crate 在 rust/ 下）**
    ```bash
    flutter_rust_bridge_codegen integrate --template plugin
    ```
    此命令会自动生成：
    - `rust/` 目录（Cargo.toml, src/lib.rs, src/api/mod.rs, src/api/simple.rs）
    - `rust_builder/` 目录（Cargokit 构建胶水，do not edit）
    - `android/` 和 `ios/` 目录的 plugin 胶水更新
    - `pubspec.yaml` 修改（添加 flutter_rust_bridge 依赖和 plugin 节）

    **Step 4: 检查并恢复 lib/flutter_mpc_wallet.dart**
    Pitfall 1 防护：integrate 可能覆盖 `lib/flutter_mpc_wallet.dart`。
    如果被覆盖，从 git 恢复：`git checkout HEAD -- lib/flutter_mpc_wallet.dart`。
    确保文件仍包含 `class FlutterMpcWalletPackage`。

    **Step 5: 配置 flutter_rust_bridge.yaml**
    创建 `flutter_rust_bridge.yaml`（如 integrate 已生成则更新），内容：
    ```yaml
    rust_input: crate::api
    dart_output: lib/src/rust
    rust_root: rust/
    dart_entrypoint_class_name: MpcBridge
    ```
    `dart_entrypoint_class_name` 设为 `MpcBridge` 而非默认的 `RustLib`，
    这样生成的初始化入口为 `MpcBridge.init()`，语义更清晰。

    **Step 6: 更新 rust/Cargo.toml**
    确保内容为：
    ```toml
    [package]
    name = "rust_lib_flutter_mpc_wallet"
    version = "0.1.0"
    edition = "2021"

    [lib]
    crate-type = ["cdylib", "staticlib"]

    [dependencies]
    flutter_rust_bridge = "=2.12.0"
    serde = { version = "1.0", features = ["derive"] }
    serde_json = "1.0"

    # Phase 3 将在此添加 kms-secp256k1（per MPC-03，当前只预留注释）
    # [dependencies.kms]
    # package = "kms"
    # git = "https://github.com/ZenGo-X/kms-secp256k1"
    # tag = "v0.3.1"
    ```
    使用 `=2.12.0` 精确版本锁定 FRB Rust 侧依赖，避免版本漂移。
    添加 serde + serde_json 为 Phase 3+ JSON payload 处理做准备。

    **Step 7: 确保 pubspec.yaml 包含正确依赖**
    - `dependencies` 下有 `flutter_rust_bridge: ^2.12.0`
    - `dev_dependencies` 下有 `integration_test` 和 `flutter_lints`（如 integrate 未添加则保留原有的）
    - 确认 `flutter.plugin` 节存在（FRB integrate 应自动添加，per D-04 只含 android + ios）

    **Step 8: 验证 Rust 编译**
    ```bash
    cd rust && cargo check && cd ..
    ```
  </action>
  <verify>
    <automated>test -f rust/Cargo.toml && test -f flutter_rust_bridge.yaml && test -d rust_builder && grep -q "flutter_rust_bridge" pubspec.yaml && grep -q "MpcBridge" flutter_rust_bridge.yaml && grep -q "FlutterMpcWalletPackage" lib/flutter_mpc_wallet.dart && (cd rust && cargo check)</automated>
  </verify>
  <acceptance_criteria>
    - `rust/Cargo.toml` 包含 `name = "rust_lib_flutter_mpc_wallet"`
    - `rust/Cargo.toml` 包含 `flutter_rust_bridge = "=2.12.0"`
    - `rust/Cargo.toml` 包含 `serde = { version = "1.0", features = ["derive"] }`
    - `rust/src/lib.rs` 存在
    - `rust/src/api/mod.rs` 存在
    - `flutter_rust_bridge.yaml` 包含 `dart_entrypoint_class_name: MpcBridge`
    - `flutter_rust_bridge.yaml` 包含 `rust_input: crate::api`
    - `pubspec.yaml` 包含 `flutter_rust_bridge:` 依赖行
    - `rust_builder/` 目录存在
    - `lib/flutter_mpc_wallet.dart` 仍包含 `class FlutterMpcWalletPackage`
    - `cd rust && cargo check` 退出码 0
  </acceptance_criteria>
  <done>FRB v2 以 plugin 模式集成到项目，Rust crate 可编译，项目配置完整，现有 Dart 代码未被破坏。</done>
</task>

<task type="auto">
  <name>Task 2: Rust DTO 定义 + 6 个 Stub 函数 + Codegen</name>
  <files>rust/src/api/types.rs, rust/src/api/mpc_engine.rs, rust/src/api/mod.rs</files>
  <read_first>
    - rust/src/api/mod.rs（Task 1 后的当前状态，可能含 FRB 默认的 simple.rs 模块声明）
    - rust/Cargo.toml（确认依赖已就绪）
    - doc/architecture/mpc_wallet_integration_plan.md（0.6 时序图 + 0.7 接口字段草案）
    - .planning/phases/01-rust-bridge-skeleton/01-RESEARCH.md（Rust Stub Function Signatures、FRB v2 Type Translation Reference 章节）
  </read_first>
  <action>
    **Step 1: 创建 rust/src/api/types.rs（per D-09: 对齐 architecture doc 字段草案）**

    定义以下 5 个 struct，字段与 `doc/architecture/mpc_wallet_integration_plan.md` 0.7 节对齐：

    ```rust
    use serde::{Deserialize, Serialize};

    /// Round-level request sent to Rust engine
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct MpcSessionRequest {
        pub session_id: String,
        pub round: u32,
        pub server_payload: Vec<u8>,
    }

    /// Round-level result from Rust engine
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct MpcRoundResult {
        pub session_id: String,
        pub round: u32,
        pub client_payload: Vec<u8>,
        pub is_complete: bool,
    }

    /// Keygen completion result — fields from architecture doc 0.7 keygen 成功响应
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct KeygenCompleteResult {
        pub mpc_key_id: String,
        pub address: String,
        pub public_key: String,
        pub curve: String,
        pub threshold: u32,
        pub key_ref: String,
        pub rotation_version: u32,
        pub local_encrypted_share: Vec<u8>,
    }

    /// Recovery completion result — fields from architecture doc 0.7 recovery 成功响应 + MPC-08
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct RecoverCompleteResult {
        pub mpc_key_id: String,
        pub address: String,
        pub public_key: String,
        pub rotation_version: u32,
        pub local_encrypted_share: Vec<u8>,
    }

    /// Sign completion result — fields from architecture doc 0.7 sign 成功响应
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct SignCompleteResult {
        pub signature: String,
        pub signed_tx: Option<String>,
        pub tx_hash: Option<String>,
    }
    ```

    **Step 2: 创建 rust/src/api/mpc_engine.rs（per D-08: 6 个 stub 函数）**

    所有 stub 返回 `Result<T, String>` — FRB 自动将 `Err` 转为 Dart Exception。
    Stub 返回 mock 数据（不用 `unimplemented!()` 或 `panic!`，避免 FFI crash）。

    ```rust
    use crate::api::types::*;

    pub fn keygen_start(server_payload: Vec<u8>) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: "stub_session".to_string(),
            round: 1,
            client_payload: vec![0u8; 32],
            is_complete: false,
        })
    }

    pub fn keygen_continue(request: MpcSessionRequest) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: request.session_id,
            round: request.round + 1,
            client_payload: vec![0u8; 32],
            is_complete: true,
        })
    }

    pub fn recover_start(server_payload: Vec<u8>) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: "stub_session".to_string(),
            round: 1,
            client_payload: vec![0u8; 32],
            is_complete: false,
        })
    }

    pub fn recover_continue(request: MpcSessionRequest) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: request.session_id,
            round: request.round + 1,
            client_payload: vec![0u8; 32],
            is_complete: true,
        })
    }

    pub fn sign_start(server_payload: Vec<u8>) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: "stub_session".to_string(),
            round: 1,
            client_payload: vec![0u8; 32],
            is_complete: false,
        })
    }

    pub fn sign_continue(request: MpcSessionRequest) -> Result<MpcRoundResult, String> {
        Ok(MpcRoundResult {
            session_id: request.session_id,
            round: request.round + 1,
            client_payload: vec![0u8; 32],
            is_complete: true,
        })
    }
    ```

    **Step 3: 更新 rust/src/api/mod.rs**
    添加模块注册（Pitfall 3 防护：未注册的模块不会被 codegen 扫描）：
    ```rust
    pub mod mpc_engine;
    pub mod types;
    ```
    保留 FRB integrate 生成的 `pub mod simple;`（如果存在）。

    **Step 4: 验证 Rust 编译**
    ```bash
    cd rust && cargo check && cd ..
    ```

    **Step 5: 运行 FRB codegen 生成 Dart 绑定**
    ```bash
    flutter_rust_bridge_codegen generate
    ```
    验证生成的 Dart 文件：
    - `lib/src/rust/api/mpc_engine.dart` 应包含 `keygenStart`, `keygenContinue` 等函数
    - `lib/src/rust/api/types.dart` 应包含 `MpcRoundResult`, `KeygenCompleteResult` 等 class
    - `lib/src/rust/frb_generated.dart` 应包含 `MpcBridge` class（因为 yaml 配了 `dart_entrypoint_class_name: MpcBridge`）

    **注意 FRB 类型映射（Pitfall 2）：**
    - `Vec<u8>` 作为 struct field → `Uint8List`（正确）
    - `Vec<u8>` 作为函数直参 → 可能是 `List<int>`（FRB 已知行为，`Uint8List` 实现了 `List<int>` 接口，功能兼容）
  </action>
  <verify>
    <automated>(cd rust && cargo check) && flutter_rust_bridge_codegen generate && test -f lib/src/rust/api/mpc_engine.dart && test -f lib/src/rust/api/types.dart</automated>
  </verify>
  <acceptance_criteria>
    - `rust/src/api/types.rs` 包含 `pub struct MpcSessionRequest`
    - `rust/src/api/types.rs` 包含 `pub struct MpcRoundResult`
    - `rust/src/api/types.rs` 包含 `pub struct KeygenCompleteResult`
    - `rust/src/api/types.rs` 包含 `pub struct RecoverCompleteResult`
    - `rust/src/api/types.rs` 包含 `pub struct SignCompleteResult`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn keygen_start`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn keygen_continue`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn recover_start`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn recover_continue`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn sign_start`
    - `rust/src/api/mpc_engine.rs` 包含 `pub fn sign_continue`
    - `rust/src/api/mod.rs` 包含 `pub mod mpc_engine` 和 `pub mod types`
    - `cd rust && cargo check` 退出码 0
    - `lib/src/rust/api/mpc_engine.dart` 存在（FRB 生成）
    - `lib/src/rust/api/types.dart` 存在（FRB 生成）
  </acceptance_criteria>
  <done>5 个 DTO struct + 6 个 stub 函数在 Rust 侧定义完毕，FRB codegen 成功生成 Dart 绑定，Rust crate 编译通过。</done>
</task>

</tasks>

<verification>
1. `cd rust && cargo check` — Rust crate 无编译错误
2. `ls lib/src/rust/api/mpc_engine.dart lib/src/rust/api/types.dart` — FRB codegen 产出存在
3. `grep -c "pub fn" rust/src/api/mpc_engine.rs` — 返回 6（6 个 stub 函数）
4. `grep "FlutterMpcWalletPackage" lib/flutter_mpc_wallet.dart` — 现有代码未被破坏
5. `grep "MpcBridge" flutter_rust_bridge.yaml` — codegen 配置正确
</verification>

<success_criteria>
- Rust crate 在 rust/ 目录下可独立编译（cargo check 通过）
- FRB codegen 成功生成 Dart 绑定到 lib/src/rust/
- 6 个 stub 函数签名与 D-08 一致
- DTO 字段与 doc/architecture/mpc_wallet_integration_plan.md 0.7 节对齐（per D-09）
- 项目仍为独立 Flutter package（per MPC-01），pubspec 未被破坏
- Phase 3 可在 Cargo.toml 中添加 kms-secp256k1 依赖（per MPC-03，预留注释）
</success_criteria>

<output>
After completion, create `.planning/phases/01-rust-bridge-skeleton/01-01-SUMMARY.md`
</output>
