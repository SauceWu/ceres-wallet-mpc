---
phase: 01-rust-bridge-skeleton
plan: 01
subsystem: infra
tags: [flutter_rust_bridge, rust, ffi, serde, cargo-ndk, stub-api]

requires: []
provides:
  - "Rust crate skeleton with FRB v2 integration"
  - "6 MPC stub functions (keygen/recover/sign start/continue)"
  - "FRB-generated Dart bindings for Rust API"
  - "CargoKit build infrastructure for Android + iOS"
affects: [01-02, 02-dart-bridge-layer, 03-mpc-crypto]

tech-stack:
  added: [flutter_rust_bridge 2.12.0, serde 1.x, serde_json 1.x, cargo-ndk 4.1.2, cargo-expand 1.0.118]
  patterns: [FFI boundary as JSON serialization layer, stub_ prefix for fake data, FRB v2 plugin template]

key-files:
  created:
    - rust/Cargo.toml
    - rust/src/api/mpc_engine.rs
    - rust/src/api/types.rs
    - rust/src/api/mod.rs
    - flutter_rust_bridge.yaml
    - lib/src/rust/api/mpc_engine.dart
    - lib/src/rust/frb_generated.dart
    - lib/src/rust/frb_generated.io.dart
    - cargokit/
    - android/
    - ios/
  modified:
    - pubspec.yaml
    - example/lib/main.dart
    - example/integration_test/simple_test.dart

key-decisions:
  - "FRB v2.12.0 plugin template used with cargokit/ (not rust_builder/) build infrastructure"
  - "All stub payloads use stub_ prefix per threat model mitigation"
  - "JSON String as FFI boundary type — MpcRoundResult serialized via serde_json"
  - "Desktop platform directories removed per D-04 (Android + iOS only)"

patterns-established:
  - "FFI boundary pattern: Rust functions accept/return String (JSON), internal types serialized via serde"
  - "Stub safety: all fake return values prefixed with stub_ to prevent real-looking secrets"
  - "FRB codegen workflow: edit Rust api/ → cargo test → flutter_rust_bridge_codegen generate → flutter analyze"

requirements-completed: [MPC-02]

duration: 7min
completed: 2026-04-08
---

# Phase 1 Plan 1: Rust Bridge Skeleton Summary

**FRB v2 plugin skeleton with 6 MPC stub functions (keygen/recover/sign), Rust unit tests, and auto-generated Dart bindings**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-08T02:28:17Z
- **Completed:** 2026-04-08T02:35:42Z
- **Tasks:** 2
- **Files modified:** 65

## Accomplishments

- Rust crate with FRB v2.12.0 integration compiles and passes all tests
- 6 MPC stub functions implemented with correct signatures per D-08
- FRB codegen generates Dart bindings with camelCase methods (keygenStart, etc.)
- All stubs return stub_-prefixed payloads (security threat mitigation verified)
- flutter analyze: zero errors; cargo clippy: zero warnings
- No cryptography libraries in dependency tree (verified via cargo tree)

## Task Commits

Each task was committed atomically:

1. **Task 1: FRB integrate + project infrastructure** - `66e5ae5` (feat)
2. **Task 2: Rust stub API + unit tests + FRB codegen** - `4f1ec44` (feat)

## Files Created/Modified

- `rust/Cargo.toml` - Rust crate config with FRB + serde dependencies
- `rust/src/api/mpc_engine.rs` - 6 MPC stub functions + 6 unit tests
- `rust/src/api/types.rs` - MpcRoundResult DTO with serde derive
- `rust/src/api/mod.rs` - API module declarations
- `flutter_rust_bridge.yaml` - FRB codegen config (rust_input, dart_output, c_output)
- `lib/src/rust/api/mpc_engine.dart` - Generated Dart bindings for 6 stub functions
- `lib/src/rust/frb_generated.dart` - Generated FRB core bindings
- `lib/src/rust/frb_generated.io.dart` - Generated FRB IO platform bindings
- `pubspec.yaml` - Added flutter_rust_bridge 2.12.0 + integration_test dev dep
- `cargokit/` - CargoKit build infrastructure (FRB v2 generated)
- `android/` - Android platform integration files
- `ios/` - iOS platform integration files + frb_generated.h
- `example/` - Example app with corrected FRB imports

## Decisions Made

- **FRB v2.12.0 uses `cargokit/` not `rust_builder/`:** Plan expected `rust_builder/` but FRB v2.12.0 generates `cargokit/` as build infrastructure directory. Functionally equivalent.
- **Kept FRB default `simple.rs` module:** Preserved the FRB template's greet() + init_app() functions alongside new MPC API for compatibility with example app.
- **Exact version pinning for FRB Rust dep:** `flutter_rust_bridge = "=2.12.0"` (generated by FRB integrate) ensures reproducible builds.
- **JSON String as FFI boundary:** All stub functions accept/return `String` (JSON serialized via serde). This pattern will carry forward to Phase 3 when real kms-secp256k1 types need isolation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed cargo-expand for FRB codegen**
- **Found during:** Task 2 (FRB codegen step)
- **Issue:** FRB codegen requires `cargo-expand` which was not installed. Latest version (1.0.121) requires rustc 1.88+, current toolchain is 1.86.0-nightly.
- **Fix:** Installed compatible version `cargo-expand 1.0.118 --locked`
- **Files modified:** None (toolchain only)
- **Verification:** FRB codegen generate completed successfully
- **Committed in:** N/A (toolchain install, not code)

**2. [Rule 1 - Bug] Fixed example app imports after FRB codegen**
- **Found during:** Task 2 (flutter analyze step)
- **Issue:** FRB-generated `example/lib/main.dart` and `example/integration_test/simple_test.dart` imported `RustLib` and `greet` from `flutter_mpc_wallet.dart` but FRB v2 places these in `src/rust/frb_generated.dart` and `src/rust/api/simple.dart`.
- **Fix:** Updated imports to correct paths
- **Files modified:** `example/lib/main.dart`, `example/integration_test/simple_test.dart`
- **Verification:** `flutter analyze` — zero errors
- **Committed in:** `4f1ec44` (part of Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both essential for build pipeline. No scope creep.

## Issues Encountered

None beyond the auto-fixed deviations above.

## Known Stubs

All 6 functions in `rust/src/api/mpc_engine.rs` are intentional stubs:
- `keygen_start` / `keygen_continue` — returns stub_keygen_* payloads
- `recover_start` / `recover_continue` — returns stub_recover_* payloads
- `sign_start` / `sign_continue` — returns stub_sign_* payloads

These will be replaced with real kms-secp256k1 logic in Phase 3. The stubs are intentional for Phase 1 (skeleton only) and do not block Phase 1's goal.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Rust crate skeleton ready for Plan 02 (Dart bridge layer / integration tests)
- FRB codegen pipeline established: edit Rust → cargo test → codegen generate → flutter analyze
- All 6 stub function signatures match D-08 specification, ready for Dart MpcEngine wrapper

## Self-Check: PASSED

All key files exist. All commits verified. SUMMARY.md created.

---
*Phase: 01-rust-bridge-skeleton*
*Completed: 2026-04-08*
