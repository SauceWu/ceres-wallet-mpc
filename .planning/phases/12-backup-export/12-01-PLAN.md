---
phase: 12-backup-export
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - rust/tests/test_backup_export.rs
autonomous: true
requirements:
  - AUX-01

must_haves:
  truths:
    - "derive_backup_envelope(keyshare_json) -> decrypt_backup_share(envelope) roundtrip restores identical Keyshare"
    - "Wrong backup secret returns Err, not panic"
    - "Truncated/corrupted payload returns Err, not panic"
  artifacts:
    - path: "rust/tests/test_backup_export.rs"
      provides: "Backup envelope integration and error path tests"
      contains: "test_backup_roundtrip"
  key_links:
    - from: "rust/tests/test_backup_export.rs"
      to: "ceres_mpc::api::mpc_engine::derive_backup_envelope"
      via: "direct function call"
      pattern: "derive_backup_envelope"
    - from: "rust/tests/test_backup_export.rs"
      to: "ceres_mpc::api::mpc_engine::decrypt_backup_share"
      via: "direct function call"
      pattern: "decrypt_backup_share"
---

<objective>
Validate that the existing AES-256-GCM backup envelope implementation works correctly with dkls23-ll Keyshare JSON serialization format. Add roundtrip integration test and error path tests.

Purpose: AUX-01 requires backup envelope to work with the new Keyshare format. The implementation is already correct (RESEARCH confirmed), but no tests exist for the dkls23-ll Keyshare path.
Output: rust/tests/test_backup_export.rs with 3 passing tests covering roundtrip + error paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-backup-export/12-RESEARCH.md

@rust/src/api/mpc_engine.rs (lines 965-1050: backup helpers + derive_backup_envelope + decrypt_backup_share)
@rust/src/api/types.rs (BackupEnvelope, DecryptBackupResult, ExportResult structs)
@rust/tests/test_dkg.rs (run_dkg_two_party helper)

<interfaces>
<!-- Key functions the executor needs -->

From rust/src/api/mpc_engine.rs:
```rust
pub fn derive_backup_envelope(
    local_encrypted_share: String,  // Keyshare JSON plaintext (NOT encrypted)
    user_backup_secret: String,
    created_at: String,
) -> Result<String, String>  // BackupEnvelope JSON

pub fn decrypt_backup_share(
    encrypted_envelope: String,  // BackupEnvelope JSON
    user_backup_secret: String,
) -> Result<String, String>  // DecryptBackupResult JSON
```

From rust/src/api/types.rs:
```rust
pub struct DecryptBackupResult {
    pub device_backup_share: String,
}
```

From rust/tests/test_dkg.rs:
```rust
pub fn run_dkg_two_party() -> (dkls23_ll::dkg::Keyshare, dkls23_ll::dkg::Keyshare);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backup envelope roundtrip + error path tests</name>
  <files>rust/tests/test_backup_export.rs</files>
  <read_first>
    - rust/src/api/mpc_engine.rs (lines 965-1050)
    - rust/src/api/types.rs (DecryptBackupResult)
    - rust/tests/test_dkg.rs (run_dkg_two_party)
  </read_first>
  <behavior>
    - test_backup_roundtrip: run_dkg_two_party() -> serde_json::to_string(share0) -> derive_backup_envelope(json, secret, timestamp) -> decrypt_backup_share(envelope, secret) -> parse DecryptBackupResult -> serde_json::from_str::<Keyshare>(device_backup_share) -> assert public_key matches original share0.public_key
    - test_backup_wrong_secret: derive_backup_envelope with "correct-secret" -> decrypt_backup_share with "wrong-secret" -> assert result.is_err(), error message contains "aes-gcm decrypt failed"
    - test_backup_truncated_payload: derive_backup_envelope -> parse BackupEnvelope JSON -> mutate payload to truncated hex (e.g., "aabb") -> re-serialize -> decrypt_backup_share -> assert result.is_err()
  </behavior>
  <action>
Create rust/tests/test_backup_export.rs with:

1. Import `mod test_dkg;` to access `run_dkg_two_party()`. Import `ceres_mpc::api::mpc_engine::{derive_backup_envelope, decrypt_backup_share}` and `ceres_mpc::api::types::{BackupEnvelope, DecryptBackupResult}`.

2. **test_backup_roundtrip:**
   - Call `test_dkg::run_dkg_two_party()` to get `(share0, _share1)`
   - `let keyshare_json = serde_json::to_string(&share0).unwrap();`
   - `let envelope_json = derive_backup_envelope(keyshare_json.clone(), "test-backup-secret".into(), "2026-01-01T00:00:00Z".into()).unwrap();`
   - `let result_json = decrypt_backup_share(envelope_json, "test-backup-secret".into()).unwrap();`
   - Parse `result_json` as `DecryptBackupResult`
   - Parse `result.device_backup_share` as `dkls23_ll::dkg::Keyshare`
   - Assert `restored.public_key == share0.public_key`

3. **test_backup_wrong_secret:**
   - Same DKG setup, derive with "correct-secret", decrypt with "wrong-secret"
   - `assert!(result.is_err())`
   - `assert!(result.unwrap_err().contains("aes-gcm decrypt failed"))`

4. **test_backup_truncated_payload:**
   - Derive envelope, parse as `BackupEnvelope`, replace `payload` with `"aabb"` (only 2 bytes when hex decoded), re-serialize, attempt decrypt
   - `assert!(result.is_err())`
  </action>
  <acceptance_criteria>
    - grep -q "fn test_backup_roundtrip" rust/tests/test_backup_export.rs
    - grep -q "fn test_backup_wrong_secret" rust/tests/test_backup_export.rs
    - grep -q "fn test_backup_truncated_payload" rust/tests/test_backup_export.rs
    - grep -q "public_key" rust/tests/test_backup_export.rs
    - grep -q "aes-gcm decrypt failed" rust/tests/test_backup_export.rs
  </acceptance_criteria>
  <verify>
    <automated>cd rust && cargo test -p ceres_mpc test_backup -- --nocapture</automated>
  </verify>
  <done>Three tests pass: roundtrip confirms Keyshare public_key preserved; wrong secret returns Err with aes-gcm message; truncated payload returns Err without panic</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_backup_secret -> HKDF | Untrusted user input becomes key material |
| encrypted_envelope -> decrypt | External data (possibly corrupted/tampered) enters decryption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | decrypt_backup_share payload | mitigate | AES-GCM AEAD tag rejects tampered ciphertext (existing implementation); test_backup_truncated_payload validates |
| T-12-02 | Info Disclosure | derive_aes_key weak secret | accept | HKDF-SHA256 stretches any-length secret; user responsibility to choose strong passphrase |
</threat_model>

<verification>
```bash
cd rust && cargo test -p ceres_mpc test_backup -- --nocapture
```
All 3 tests pass. No panics on error paths.
</verification>

<success_criteria>
- test_backup_roundtrip: Keyshare JSON -> encrypt -> decrypt -> deserialize -> public_key matches
- test_backup_wrong_secret: wrong key returns Err (not panic)
- test_backup_truncated_payload: truncated payload returns Err (not panic)
- All 3 tests green in cargo test
</success_criteria>

<output>
After completion, create `.planning/phases/12-backup-export/12-01-SUMMARY.md`
</output>
