---
phase: 04-real-signing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/src/dto/mpc_dtos.dart
  - lib/src/client/mpc_client.dart
  - test/dto/mpc_dtos_test.dart
  - test/client/mpc_client_test.dart
autonomous: true
requirements: [MPC-04, MPC-09, MPC-10]

must_haves:
  truths:
    - "SignResult 字段完全替换为 r(String), s(String), recid(int)，旧字段 signature/signedTx/txHash 删除"
    - "MpcClient.sign(mpcKeyId, messageHash, localEncryptedShare) 返回 SignResult — 精确匹配 D-01 签名"
    - "MpcClient.sign() 遵循 round-trip 模式：transport /sign/start → Rust signStart → transport /sign/continue → Rust signContinue → SignResult"
    - "当服务端 status=completed 时，MpcClient.sign() 直接从服务端响应解析 {r, s, recid} 作为 SignResult"
    - "flutter test test/client/mpc_client_test.dart 和 test/dto/mpc_dtos_test.dart 全部通过"
  artifacts:
    - path: "lib/src/dto/mpc_dtos.dart"
      provides: "SignResult { r, s, recid } — 旧字段已删除"
      contains: "final String r"
    - path: "lib/src/client/mpc_client.dart"
      provides: "Future<SignResult> sign({mpcKeyId, messageHash, localEncryptedShare})"
      exports: ["sign"]
    - path: "test/client/mpc_client_test.dart"
      provides: "group('sign', ...) — mock 测试覆盖 round-trip、completed-from-server、error、transport failure"
      contains: "group('sign'"
    - path: "test/dto/mpc_dtos_test.dart"
      provides: "SignResult r/s/recid 字段断言"
      contains: "SignResult"
  key_links:
    - from: "MpcClient.sign()"
      to: "_engine.signStart(sessionId, localEncryptedShare, serverPayloadJson)"
      via: "round 1 Rust call with share"
      pattern: "signStart"
    - from: "server completed response"
      to: "SignResult.fromJson(serverData)"
      via: "服务端 status=completed 时直接解析 {r, s, recid}"
      pattern: "SignResult.fromJson"
    - from: "SignResult.fromJson"
      to: "json\\['r'\\], json\\['s'\\], json\\['recid'\\]"
      via: "精确字段名（小写，与 Rust SignCompletedPayload 匹配）"
      pattern: "json\\['r'\\]"
---

<objective>
更新 Dart 层：将 SignResult DTO 字段替换为 r/s/recid，为 MpcClient 增加 sign() 方法，补全对应测试。

Purpose: 完成 Phase 4 的 Dart 侧闭环——MpcClient 可驱动完整签名 round-trip，SignResult 携带调用者组装 signedTx 所需的全部字段。

Output:
- mpc_dtos.dart: SignResult 字段从 signature/signedTx/txHash 替换为 r/s/recid（per D-02）
- mpc_client.dart: sign() 方法（per D-01, D-03, D-06）
- mpc_dtos_test.dart: SignResult 字段断言更新
- mpc_client_test.dart: group('sign', ...) mock 测试新增
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-real-signing/04-CONTEXT.md
@.planning/phases/04-real-signing/04-RESEARCH.md
@.planning/phases/04-real-signing/04-01-SUMMARY.md

<interfaces>
<!-- 从现有代码提取，executor 无需额外探索 -->

From lib/src/bridge/mpc_engine.dart (MpcEngine 已有 signStart/signContinue，无需修改):
```dart
Future<MpcRoundResult> signStart(
  String sessionId,
  String share,
  String serverPayload,
) async { ... }

Future<MpcRoundResult> signContinue(
  String sessionId,
  String serverPayload,
) async { ... }
```

From lib/src/client/mpc_client.dart (复用的 private helpers):
```dart
Future<String> _sendToServer(String endpoint, String payload)
Map<String, dynamic> _parseServerResponse(String response)
void _checkProtocolError(MpcRoundResult result)
Map<String, dynamic> _snakeToCamelKeys(Map<String, dynamic> map)
```

From lib/src/client/mpc_client.dart (recover() 编排模式参考):
```dart
Future<RecoveryResult> recover({
  required String mpcKeyId,
  required String encryptedBackupShare,
  required String userBackupSecret,
}) async {
  // initResponse → sessionId
  // engine.recoverStart(sessionId, backupShare, serverPayload)
  // while isContinue → /recovery/continue → engine.recoverContinue
  // serverData['status'] == 'completed' → RecoveryResult.fromJson(serverData)
  // isCompleted + clientPayload → RecoveryResult.fromJson(_snakeToCamelKeys(payload))
}
```

From test/client/mpc_client_test.dart (mock 模式参考):
```dart
class MockMpcEngine extends Mock implements MpcEngine {}
class MockMpcTransport extends Mock implements MpcTransport {}

// mock 用法：
when(() => mockTransport.send('/keygen/start', any()))
    .thenAnswer((_) async => jsonEncode({...}));
when(() => mockEngine.keygenStart('sess_kg1', any()))
    .thenAnswer((_) async => const MpcRoundResult(...));
```

From lib/src/dto/mpc_dtos.dart (SignResult 当前字段 — 需完全替换):
```dart
// 当前（需删除）:
final String? signature;
final String? signedTx;
final String? txHash;

// 替换为（per D-02）:
final String r;
final String s;
final int recid;
```

JSON key 映射（Rust SignCompletedPayload → Dart SignResult.fromJson）：
- Rust: `pub r: String` → JSON key: `"r"` → Dart: `json['r'] as String`
- Rust: `pub s: String` → JSON key: `"s"` → Dart: `json['s'] as String`
- Rust: `pub recid: u8` → JSON key: `"recid"` → Dart: `json['recid'] as int`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 SignResult DTO + 新增 MpcClient.sign()</name>
  <files>lib/src/dto/mpc_dtos.dart, lib/src/client/mpc_client.dart</files>
  <behavior>
    - SignResult 构造函数参数：required String r, required String s, required int recid（全部 required，非 nullable）
    - SignResult.fromJson 从 json['r'], json['s'], json['recid'] 解析
    - SignResult.toString 不包含 r/s/recid 的实际值（签名组件属于敏感数据，用 [REDACTED]）
    - MpcClient.sign() 签名：Future&lt;SignResult&gt; sign({required String mpcKeyId, required String messageHash, required String localEncryptedShare})
    - sign() 内部：/sign/start 请求体包含 mpcKeyId 和 messageHash（服务端需要 messageHash 来生成 SignRound1ServerPayload）
    - signStart 调用：_engine.signStart(sessionId, localEncryptedShare, jsonEncode(initData['serverPayload']))
    - 当服务端返回 status=completed 时：SignResult.fromJson(serverData)（服务端直接返回 {r, s, recid}）
    - 当 Rust signContinue 返回 isCompleted 时：客户端通常不会走这条路（sign 由服务端完成），但需处理边界情况
  </behavior>
  <action>
**mpc_dtos.dart — 替换 SignResult 类（完整替换，删除旧字段）：**

```dart
/// Sign completion result per D-02.
/// Contains ECDSA signature components: r, s, recid.
/// Caller is responsible for assembling signedTx.
class SignResult {
  final String r;
  final String s;
  final int recid;

  const SignResult({
    required this.r,
    required this.s,
    required this.recid,
  });

  factory SignResult.fromJson(Map<String, dynamic> json) {
    return SignResult(
      r: json['r'] as String,
      s: json['s'] as String,
      recid: json['recid'] as int,
    );
  }

  @override
  String toString() {
    return 'SignResult(r: [REDACTED], s: [REDACTED], recid: $recid)';
  }
}
```

**mpc_client.dart — 在 recover() 方法之后、_sendToServer 之前插入 sign() 方法：**

```dart
/// Execute full sign protocol.
///
/// Drives two-party ECDSA signing using [MpcEngine] (Rust FFI) and
/// [MpcTransport] (host-injected network).
///
/// Per D-01: [messageHash] is raw 32-byte hash hex (no 0x prefix).
/// Per D-03: [localEncryptedShare] is passed by caller — SDK is stateless.
/// Per D-02: Returns [SignResult] with r, s, recid for caller to assemble signedTx.
Future<SignResult> sign({
  required String mpcKeyId,
  required String messageHash,
  required String localEncryptedShare,
}) async {
  // Send mpcKeyId + messageHash to server so it can generate Party1 ephemeral
  // first message and include messageHash in serverPayload for session storage
  final initResponse = await _sendToServer(
    '/sign/start',
    jsonEncode({'mpcKeyId': mpcKeyId, 'messageHash': messageHash}),
  );
  final initData = _parseServerResponse(initResponse);
  final sessionId = initData['sessionId'] as String;

  // Party2 round 1: Rust generates ephemeral key pair
  final round1 = await _engine.signStart(
    sessionId,
    localEncryptedShare,
    jsonEncode(initData['serverPayload']),
  );
  _checkProtocolError(round1);

  var currentResult = round1;
  while (currentResult.isContinue) {
    final serverResponse = await _sendToServer(
      '/sign/continue',
      jsonEncode({
        'sessionId': sessionId,
        'round': currentResult.round,
        'clientPayload': currentResult.clientPayload,
      }),
    );
    final serverData = _parseServerResponse(serverResponse);

    // Server (Party1) completes signing and returns { r, s, recid }
    if (serverData['status'] == 'completed') {
      return SignResult.fromJson(serverData);
    }

    currentResult = await _engine.signContinue(
      sessionId,
      jsonEncode(serverData['serverPayload'] ?? {}),
    );
    _checkProtocolError(currentResult);
  }

  // Rust side completed (edge case: local completion without server roundtrip)
  if (currentResult.isCompleted && currentResult.clientPayload != null) {
    final payload =
        jsonDecode(currentResult.clientPayload!) as Map<String, dynamic>;
    return SignResult.fromJson(payload);
  }

  throw MpcProtocolException('Unexpected sign state: ${currentResult.status}');
}
```
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet && flutter analyze lib/src/dto/mpc_dtos.dart lib/src/client/mpc_client.dart 2>&1 | tail -5</automated>
  </verify>
  <done>
  - flutter analyze 无错误
  - SignResult 类只包含 r, s, recid 字段（无 signature/signedTx/txHash）
  - MpcClient 包含 sign() 方法
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 DTO 测试 + 新增 MpcClient sign 测试</name>
  <files>test/dto/mpc_dtos_test.dart, test/client/mpc_client_test.dart</files>
  <behavior>
    - mpc_dtos_test.dart: 删除旧 SignResult 测试（如有），新增测试：fromJson 解析 r/s/recid；toString 包含 recid 但不暴露 r/s 原始值（含 [REDACTED]）
    - mpc_client_test.dart group('sign'): 覆盖以下场景：
      1. 服务端 completed: server 在 /sign/continue 响应中返回 status=completed + {r, s, recid} → SignResult 正确
      2. round-trip: sign_start → transport continue → server completed 完整流程 mock
      3. transport failure: _sendToServer 抛出异常 → MpcTransportException
      4. protocol error: Rust signStart 返回 status=error → MpcProtocolException
  </behavior>
  <action>
**test/dto/mpc_dtos_test.dart — 在文件末尾追加 SignResult 测试 group（保留现有所有测试）：**

```dart
group('SignResult', () {
  test('fromJson parses r, s, recid correctly', () {
    final json = {'r': 'aabbcc', 's': 'ddeeff', 'recid': 0};
    final result = SignResult.fromJson(json);
    expect(result.r, 'aabbcc');
    expect(result.s, 'ddeeff');
    expect(result.recid, 0);
  });

  test('toString redacts r and s, exposes recid', () {
    final result = SignResult(r: 'SECRET_R', s: 'SECRET_S', recid: 1);
    final s = result.toString();
    expect(s, contains('[REDACTED]'));
    expect(s, isNot(contains('SECRET_R')));
    expect(s, isNot(contains('SECRET_S')));
    expect(s, contains('recid: 1'));
  });
});
```

**test/client/mpc_client_test.dart — 在最后一个 group 之后追加 sign group：**

```dart
group('sign', () {
  const sessionId = 'sess_sign1';
  const signServerPayload = {'eph_key_gen_first_message': 'p1_eph', 'message_hash': 'aa' * 32};

  test('completes sign when server returns completed in /sign/continue', () async {
    when(() => mockTransport.send('/sign/start', any()))
        .thenAnswer((_) async => jsonEncode({
              'sessionId': sessionId,
              'serverPayload': signServerPayload,
            }));

    when(() => mockEngine.signStart(sessionId, 'live_share_json', any()))
        .thenAnswer((_) async => const MpcRoundResult(
              status: 'continue',
              round: 1,
              clientPayload: '{"eph_key_gen_first_message_party_two": "p2_eph"}',
            ));

    // Server returns completed with r, s, recid
    when(() => mockTransport.send('/sign/continue', any()))
        .thenAnswer((_) async => jsonEncode({
              'status': 'completed',
              'r': 'deadbeef',
              's': 'cafebabe',
              'recid': 0,
            }));

    final result = await client.sign(
      mpcKeyId: 'key_001',
      messageHash: 'aa' * 32,
      localEncryptedShare: 'live_share_json',
    );

    expect(result.r, 'deadbeef');
    expect(result.s, 'cafebabe');
    expect(result.recid, 0);
  });

  test('throws MpcTransportException when /sign/start transport fails', () async {
    when(() => mockTransport.send('/sign/start', any()))
        .thenThrow(Exception('network error'));

    expect(
      () => client.sign(
        mpcKeyId: 'key_001',
        messageHash: 'aa' * 32,
        localEncryptedShare: 'live_share',
      ),
      throwsA(isA<MpcTransportException>()),
    );
  });

  test('throws MpcProtocolException when Rust signStart returns error', () async {
    when(() => mockTransport.send('/sign/start', any()))
        .thenAnswer((_) async => jsonEncode({
              'sessionId': sessionId,
              'serverPayload': signServerPayload,
            }));

    when(() => mockEngine.signStart(any(), any(), any()))
        .thenAnswer((_) async => const MpcRoundResult(
              status: 'error',
              round: 1,
              errorMessage: 'invalid message hash length',
            ));

    expect(
      () => client.sign(
        mpcKeyId: 'key_001',
        messageHash: 'bad_hash',
        localEncryptedShare: 'live_share',
      ),
      throwsA(isA<MpcProtocolException>()),
    );
  });

  test('throws MpcProtocolException when Rust signContinue returns error', () async {
    when(() => mockTransport.send('/sign/start', any()))
        .thenAnswer((_) async => jsonEncode({
              'sessionId': sessionId,
              'serverPayload': signServerPayload,
            }));

    when(() => mockEngine.signStart(any(), any(), any()))
        .thenAnswer((_) async => const MpcRoundResult(
              status: 'continue',
              round: 1,
              clientPayload: 'p2_round1',
            ));

    when(() => mockTransport.send('/sign/continue', any()))
        .thenAnswer((_) async => jsonEncode({
              'serverPayload': {'round': 2},
            }));

    when(() => mockEngine.signContinue(any(), any()))
        .thenAnswer((_) async => const MpcRoundResult(
              status: 'error',
              round: 2,
              errorMessage: 'session not found',
            ));

    expect(
      () => client.sign(
        mpcKeyId: 'key_001',
        messageHash: 'aa' * 32,
        localEncryptedShare: 'live_share',
      ),
      throwsA(isA<MpcProtocolException>()),
    );
  });
});
```

**注意：** `'aa' * 32` 在 Dart 中是字符串重复语法（`'aa' * 32` = 64 个字符的 hex 串），如果 Dart 不支持此语法，改用字面量 `'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa'`（64 个 a）。

另：`signServerPayload` 中的 `'eph_key_gen_first_message': 'p1_eph'` 是 mock 数据，test 不校验其内容（mockEngine.signStart 接受 any()）。
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet && flutter test test/dto/mpc_dtos_test.dart test/client/mpc_client_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>
  - flutter test 全部通过（含新增 sign group 的 4 个测试 + SignResult DTO 的 2 个测试）
  - 无旧的 signature/signedTx/txHash 引用
  - MpcTransportException 和 MpcProtocolException 场景均有测试覆盖
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → MpcClient.sign() | messageHash 和 localEncryptedShare 来自调用方，SDK 不校验语义 |
| server response → SignResult | 服务端 JSON 中的 r/s/recid 直接解析为 SignResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | SignResult.toString | mitigate | toString 对 r/s 使用 [REDACTED]，recid 可见（非敏感，仅 0 或 1）|
| T-04-07 | Tampering | SignResult.fromJson | accept | 强类型解析：r/s as String, recid as int；非预期类型会抛出 TypeError |
| T-04-08 | Repudiation | sign() transport | accept | transport 层由 host 注入，SDK 不持有网络连接状态 |
| T-04-09 | Information Disclosure | localEncryptedShare in logs | accept | share 作为参数传入 signStart，MpcEngine 不打印参数；host 注入的 transport 负责自身日志安全 |
</threat_model>

<verification>
全量验证（Task 2 完成后运行）：

```bash
cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet
flutter test 2>&1 | tail -10
```

期望：All tests passed。

同时确认 SignResult 无旧字段：
```bash
cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet
grep -n "signedTx\|txHash\|signature" lib/src/dto/mpc_dtos.dart
```

期望：无输出（旧字段已删除）。
</verification>

<success_criteria>
- flutter analyze 无错误
- flutter test 全部通过（含 sign group 4 个 + SignResult DTO 2 个）
- mpc_dtos.dart 中 SignResult 只包含 r, s, recid（无 signature/signedTx/txHash）
- MpcClient.sign() 存在且签名匹配 D-01（mpcKeyId, messageHash, localEncryptedShare）
- SignResult.toString 包含 [REDACTED] 不暴露 r/s 值
</success_criteria>

<output>
完成后创建 `.planning/phases/04-real-signing/04-02-SUMMARY.md`，记录：
- SignResult 字段变更（旧 → 新）
- MpcClient.sign() 方法签名
- MpcClient.sign() 与服务端的 wire 交互格式（/sign/start 请求体、/sign/continue 响应格式）
- 新增测试用例列表
- flutter test 结果摘要
</output>
