---
phase: 13.1-sl-dkls23-migration
plan: 04
type: execute
wave: 4
depends_on: ["13.1-03"]
files_modified:
  - rust/src/frb_generated.rs
  - lib/src/bridge/frb_generated.dart
  - lib/src/bridge/frb_generated.io.dart
  - lib/src/bridge/frb_generated.web.dart
  - lib/src/bridge/api/mpc_engine.dart
  - lib/src/bridge/api/types.dart
autonomous: false
requirements:
  - INFRA-02
  - INFRA-05
  - REG-02

must_haves:
  truths:
    - "FRB codegen runs without errors"
    - "cargo check --target aarch64-apple-ios passes"
    - "cargo check --target aarch64-linux-android passes"
    - "flutter analyze passes"
    - "Dart bridge API signatures unchanged"
  artifacts:
    - path: "rust/src/frb_generated.rs"
      provides: "Regenerated FRB Rust bridge"
    - path: "lib/src/bridge/frb_generated.dart"
      provides: "Regenerated FRB Dart bridge"
  key_links:
    - from: "lib/src/bridge/api/mpc_engine.dart"
      to: "rust/src/api/mpc_engine.rs"
      via: "FRB codegen binding"
      pattern: "keygenStart|signStart|recoverStart"
---

<objective>
Run FRB codegen to regenerate Dart bindings, verify iOS + Android cross-compilation, and verify flutter analyze passes. Human verify checkpoint for end-to-end confidence.

Purpose: Ensure the sl-dkls23 migration is complete — Rust compiles for mobile targets, Dart bindings are regenerated, and the full stack is consistent.

Output: Regenerated FRB bindings, verified cross-compilation, flutter analyze clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13.1-sl-dkls23-dkls23-ll-sl-dkls23-async-api-channelrelay-http-st/13.1-CONTEXT.md
@.planning/phases/13.1-sl-dkls23-dkls23-ll-sl-dkls23-async-api-channelrelay-http-st/13.1-03-SUMMARY.md

<interfaces>
<!-- FRB codegen command from Phase 13 -->
```bash
cd rust && flutter_rust_bridge_codegen generate 2>&1
```

<!-- Cross-compilation targets -->
```bash
cargo check --target aarch64-apple-ios 2>&1
cargo check --target aarch64-linux-android 2>&1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FRB codegen + cross-compilation check</name>
  <files>rust/src/frb_generated.rs, lib/src/bridge/frb_generated.dart, lib/src/bridge/frb_generated.io.dart, lib/src/bridge/frb_generated.web.dart, lib/src/bridge/api/mpc_engine.dart, lib/src/bridge/api/types.dart</files>
  <read_first>
    - rust/src/api/mpc_engine.rs (verify all pub fn signatures are valid)
    - rust/src/frb_generated.rs (first 30 lines — understand current state)
    - lib/src/bridge/api/mpc_engine.dart (understand current Dart API)
  </read_first>
  <action>
**Step 1: Run FRB codegen**

```bash
cd rust && flutter_rust_bridge_codegen generate
```

This regenerates:
- `rust/src/frb_generated.rs`
- `lib/src/bridge/frb_generated.dart`
- `lib/src/bridge/frb_generated.io.dart`
- `lib/src/bridge/frb_generated.web.dart`
- `lib/src/bridge/api/mpc_engine.dart`
- `lib/src/bridge/api/types.dart`

If codegen fails, check:
- All `pub fn` in mpc_engine.rs have types that FRB can handle (String, Result<String, String>)
- No new types that FRB doesn't recognize
- The function signatures haven't changed parameter types

**Step 2: Verify cross-compilation**

```bash
cd rust && cargo check --target aarch64-apple-ios 2>&1 | tail -10
cd rust && cargo check --target aarch64-linux-android 2>&1 | tail -10
```

Both must pass without errors. Common issues:
- sl-dkls23 may have platform-specific dependencies
- tokio features may need adjustment for mobile targets
- If `aarch64-linux-android` fails, check if `ANDROID_NDK_HOME` is set and .cargo/config.toml has the linker configured

**Step 3: Flutter analyze**

```bash
flutter analyze 2>&1 | tail -20
```

Must pass with no errors (warnings acceptable).

**Step 4: Verify Dart API unchanged**

After codegen, check that `lib/src/bridge/api/mpc_engine.dart` still has:
- `keygenStart(sessionId, serverPayload)`
- `keygenContinue(sessionId, serverPayload)`
- `signStart(sessionId, share, messageHashHex, serverPayload)`
- `signContinue(sessionId, serverPayload)`
- `recoverStart(sessionId, backupShare, serverPayload, currentRotationVersion)`
- `recoverContinue(sessionId, serverPayload)`
- `deriveBackupEnvelope(localEncryptedShare, userBackupSecret, createdAt)`
- `decryptBackupShare(encryptedEnvelope, userBackupSecret)`
- `exportPrivateKey(localShare, serverSharePrivate)`

  </action>
  <verify>
    <automated>cd rust && cargo check --target aarch64-apple-ios 2>&1 | tail -5 && cargo check --target aarch64-linux-android 2>&1 | tail -5 && cd .. && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_rust_bridge_codegen generate` exits 0
    - `cargo check --target aarch64-apple-ios` exits 0
    - `cargo check --target aarch64-linux-android` exits 0
    - `flutter analyze` reports no errors
    - lib/src/bridge/api/mpc_engine.dart contains `keygenStart`
    - lib/src/bridge/api/mpc_engine.dart contains `signStart`
    - lib/src/bridge/api/mpc_engine.dart contains `recoverStart`
    - lib/src/bridge/api/mpc_engine.dart contains `exportPrivateKey`
    - rust/src/frb_generated.rs exists and is non-empty
  </acceptance_criteria>
  <done>FRB codegen successful, iOS + Android cross-compilation passes, flutter analyze clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify migration completeness</name>
  <files>N/A</files>
  <action>
Human verifies the complete sl-dkls23 migration by running test suite and checking for residual dkls23-ll references.
  </action>
  <what-built>
Complete sl-dkls23 migration:
1. Cargo.toml: dkls23-ll replaced with sl-dkls23 + sl-mpc-mate + tokio
2. New files: runtime.rs (OnceCell tokio Runtime), relay.rs (ChannelRelayConn)
3. Rewritten: session.rs (channel-based), mpc_engine.rs (async ChannelRelay bridge)
4. All 4 integration tests rewritten with #[tokio::test]
5. FRB codegen regenerated, iOS/Android cross-compilation verified
  </what-built>
  <how-to-verify>
1. Run full test suite: `cd rust && cargo test 2>&1`
   - Expected: ALL tests pass (test_dkg, test_dsg, test_rotation, test_backup_export, unit tests)

2. Verify no dkls23-ll references remain:
   ```bash
   grep -r "dkls23_ll\|dkls23-ll" rust/src/ rust/tests/ rust/Cargo.toml
   ```
   - Expected: NO matches

3. Verify sl-dkls23 is the protocol library:
   ```bash
   grep "sl-dkls23\|sl_dkls23" rust/Cargo.toml rust/src/api/mpc_engine.rs
   ```
   - Expected: Multiple matches

4. Run flutter analyze: `flutter analyze`
   - Expected: No errors
  </how-to-verify>
  <verify>Human confirms all checks pass</verify>
  <done>Human approved: all tests pass, zero dkls23-ll references, flutter analyze clean</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FRB codegen | Generated code bridges Dart to Rust; must preserve function signatures |
| Cross-compilation | Mobile targets may have different linking requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-11 | Tampering | frb_generated.rs | accept | FRB codegen is deterministic from pub fn signatures; regenerated from trusted source |
| T-13.1-12 | Denial of Service | cross-compilation | accept | cargo check only — no runtime risk; build failures are caught in CI |
</threat_model>

<verification>
- FRB codegen runs without errors
- `cargo check` passes for both mobile targets
- `flutter analyze` passes
- No dkls23-ll references remain anywhere in the codebase
- Human approves final verification
</verification>

<success_criteria>
- FRB Dart bindings regenerated and consistent with Rust API
- iOS (aarch64-apple-ios) and Android (aarch64-linux-android) cargo check pass
- flutter analyze reports no errors
- Human verified full test suite passes
- Zero dkls23-ll references in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-sl-dkls23-dkls23-ll-sl-dkls23-async-api-channelrelay-http-st/13.1-04-SUMMARY.md`
</output>
