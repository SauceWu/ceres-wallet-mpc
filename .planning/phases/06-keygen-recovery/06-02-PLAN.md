---
phase: 06-keygen-recovery
plan: 06-02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/service/address_service.dart
  - lib/context/setup/wallet_setup_handler.dart
  - lib/data/db/dao/account_dao.dart
  - lib/data/db/dao/account_address_dao.dart
  - lib/data/remote/http.dart
  - lib/data/remote/mpc/mpc_models.dart
  - test/address_service_injection_test.dart
  - test/db_security_baseline_test.dart
  - test/mpc_signer_evm_flow_test.dart
  - test/security/loggable_http_client_test.dart
  - doc/integration.md
  - doc/architecture/mpc_wallet_integration_plan.md
autonomous: true
requirements:
  - TSS-02
must_haves:
  truths:
    - "setupMpcWallet/recoverMpcWallet 不再生成、派生或持久化本地完整私钥。"
    - "recovery 输入只接受 mpcKeyId，后端返回 address/publicKey/localEncryptedShare/rotationVersion 为权威结果。"
    - "Phase 6 必须完成一次真实 create/recover 成功闭环验证，不能以“暂不可用”作为收尾状态。"
    - "新 sensitive fields 的日志脱敏、测试与主流程回归门禁同步更新。"
  artifacts:
    - path: lib/service/address_service.dart
      provides: "真实 backend-authoritative EVM keygen/recovery 编排"
    - path: lib/data/db/dao/account_address_dao.dart
      provides: "MPC 行 metadata-only 写库边界"
    - path: doc/integration.md
      provides: "宿主接入说明：EVM-only、mpcKeyId-only recovery、secure share storage"
  key_links:
    - from: lib/context/setup/wallet_setup_handler.dart
      to: lib/service/address_service.dart
      via: "mpcKeyId-only recovery input and backend-authoritative create/recover orchestration"
      pattern: "recoverMpcWallet\\(|setupMpcWallet\\("
    - from: lib/service/address_service.dart
      to: lib/data/db/dao/account_dao.dart
      via: "account content remains type marker only"
      pattern: "content: 'mpc:"
    - from: lib/service/address_service.dart
      to: lib/data/db/dao/account_address_dao.dart
      via: "metadata-only MPC address persistence"
      pattern: "privateKey: null|backupState|rotationVersion"
    - from: lib/data/remote/mpc/mpc_api.dart
      to: lib/data/remote/http.dart
      via: "new sensitive create/recover payloads continue through redacted HTTP transport"
      pattern: "httpclient\\.post"
---

# 06-02-PLAN

## objective
用真实 backend-authoritative 的 EVM keygen/recovery 编排替换旧伪实现，并补齐脱敏、测试与主流程回归门禁。

## tasks
<tasks>
  <task id="06-02-00">
    <name>Task 0: Lock backend readiness contract and unavailable fallback gate</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      lib/context/setup/wallet_setup_handler.dart
      lib/service/address_service.dart
      lib/data/remote/mpc/mpc_models.dart
      lib/data/remote/mpc/mpc_api.dart
      test/address_service_injection_test.dart
    </read_first>
    <files>
      lib/context/setup/wallet_setup_handler.dart
      lib/service/address_service.dart
      lib/data/remote/mpc/mpc_models.dart
      test/address_service_injection_test.dart
    </files>
    <action>
      先把 Phase 6 的 backend readiness 写成明确契约与验收入口。`keygen/recovery` success payload 必需字段固定为 `mpcKeyId`、`address`、`publicKey`、`curve`、`threshold`、`keyRef`、`backupState`、`rotationVersion`、`localEncryptedShare`。本任务除失败兜底外，还必须补一组“真实协议 happy path”编排测试：通过注入式 fake/fixture backend 返回完整 success payload，验证 create 与 recover 都能完成一次权威闭环并把 `localEncryptedShare` 写入 `MpcShareStore`。只有 backend create/recover 返回缺字段、鉴权失败、接口不可用时，`wallet_setup_handler.dart` 和 `address_service.dart` 才统一走“暂不可用/稍后重试”的宿主可观测错误路径，且绝不能回退到旧伪实现。补齐错误语义：至少区分 backend unavailable、bad response、secure-share-store write failure、local verify mismatch、unsupported chain/no EVM chain；这些失败路径必须保证不落库、不写 share、不刷新账户列表。
    </action>
    <acceptance_criteria>
      `rg -n "localEncryptedShare|rotationVersion|backupState" lib/data/remote/mpc/mpc_models.dart` 命中 success payload 必需字段；`rg -n "暂不可用|稍后重试|badResponse|verify mismatch|unsupported" lib/context/setup/wallet_setup_handler.dart lib/service/address_service.dart` 命中明确错误映射；`flutter test test/address_service_injection_test.dart -r expanded` 中同时存在 successful create/recover 闭环断言和 backend unavailable / missing fields / secure storage fail / verify fail 不落库断言，且退出码为 0。
    </acceptance_criteria>
    <verify>
      flutter test test/address_service_injection_test.dart -r expanded
    </verify>
    <done>
      Phase 6 已锁定真实 create/recover success contract，并且 happy path 与失败兜底都不会再掉回旧伪实现。
    </done>
  </task>

  <task id="06-02-01">
    <name>Task 1: Replace fake create/recover orchestration with backend-authoritative flow</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      .planning/phases/06-keygen-recovery/06-01-PLAN.md
      lib/service/address_service.dart
      lib/context/setup/wallet_setup_handler.dart
      lib/data/db/dao/account_dao.dart
      lib/data/db/dao/account_address_dao.dart
      lib/data/remote/mpc/mpc_models.dart
      lib/data/remote/mpc/mpc_api.dart
      lib/data/security/mpc_share_store.dart
    </read_first>
    <files>
      lib/service/address_service.dart
      lib/context/setup/wallet_setup_handler.dart
      lib/data/db/dao/account_dao.dart
      lib/data/db/dao/account_address_dao.dart
      lib/data/remote/mpc/mpc_models.dart
      test/address_service_injection_test.dart
    </files>
    <action>
      按已拍板前提实现真实 create/recover 编排：`recovery` 成功响应必须包含新的 `localEncryptedShare + rotationVersion + address/publicKey`，并以此作为 Phase 6 的权威契约。删除 `setupMpcWallet` 中 `generateMnemonic()/getPrivateKey()` 路径，删除 `recoverMpcWallet` 中任何 `sha256(mpcKeyId)` 或等价伪恢复逻辑。`wallet_setup_handler.dart` 的 recovery 输入改成只传 `mpcKeyId`。`address_service.dart` 中创建/恢复仅筛选 `chainId == 60` 的 EVM 链，并在本地做公钥-地址校验，失败则显式报错且不落库。这里只允许改调用层与 DAO 写库边界，不新增 Drift schema：`account_address_dao.dart` 必须直接支持 MPC 行 `privateKey == null`，只保存 `address`、`mpcKeyId/keyRef`、`curve`、`threshold`、`publicKey`、`backupState`、`rotationVersion` 和必要 metadata；`localEncryptedShare` 仅进入 `MpcShareStore`。同时补齐 `account_dao.dart` / `address_service.dart` 的账户内容语义约束，确保 `content: 'mpc:...'` 仅作账户类型标识，不拼接、推导或暗示任何本地密钥材料。
    </action>
    <acceptance_criteria>
      `rg -n "generateMnemonic\\(|sha256\\.convert" lib/service/address_service.dart` 不再命中 MPC create/recover 路径；`rg -n "mpcKeyId:" lib/context/setup/wallet_setup_handler.dart` 命中 recovery-only 输入而不再要求 `address`；`rg -n "backupState|rotationVersion|publicKey" lib/service/address_service.dart lib/data/db/dao/account_address_dao.dart` 命中新 metadata 落库；`rg -n "content: 'mpc:" lib/service/address_service.dart lib/data/db/dao/account_dao.dart` 命中类型标识写入但无密钥材料拼接；`flutter test test/address_service_injection_test.dart -r expanded` 退出码为 0。
    </acceptance_criteria>
    <verify>
      flutter test test/address_service_injection_test.dart -r expanded
    </verify>
    <done>
      旧伪 MPC 创建/恢复实现已经被真实后端权威编排替换，且无需改 schema 就满足本轮 recovery 契约。
    </done>
  </task>

  <task id="06-02-02">
    <name>Task 2: Update redaction, security baselines, and main-flow regression gates</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      lib/data/remote/http.dart
      test/security/loggable_http_client_test.dart
      test/db_security_baseline_test.dart
      test/mpc_signer_evm_flow_test.dart
      lib/data/remote/http.dart
      test/security/loggable_http_client_test.dart
      doc/integration.md
      doc/architecture/mpc_wallet_integration_plan.md
      tool/example_regression.sh
    </read_first>
    <files>
      lib/data/remote/http.dart
      test/security/loggable_http_client_test.dart
      test/db_security_baseline_test.dart
      test/mpc_signer_evm_flow_test.dart
      doc/integration.md
      doc/architecture/mpc_wallet_integration_plan.md
    </files>
    <action>
      把 `localEncryptedShare`、keygen/recovery challenge/payload、新增 `mpcMetadata` 字段明确纳入敏感数据边界。显式修改 `lib/data/remote/http.dart` 与 `test/security/loggable_http_client_test.dart`，验证 `MpcApi` 继续复用统一红线 HTTP client，不新增裸请求路径，并确保这些字段不会出现在日志中。同步更新 `db_security_baseline_test.dart`，断言 Drift 不保存 MPC `privateKey`。更新 `doc/integration.md` 与 `doc/architecture/mpc_wallet_integration_plan.md`，明确声明本阶段仅支持 EVM、采用对标 OKX/Binance 的 2-of-3 mobile MPC、本地 live share 仅在 secure storage 中按 `mpcKeyId` 保存、recovery 输入为 `mpcKeyId` only 且后端返回权威结果。最后把主流程门禁固化为 `flutter analyze`、相关 MPC 测试、以及 `./tool/example_regression.sh`，不能只停在局部单测。
    </action>
    <acceptance_criteria>
      文档中出现 `EVM only`、`2-of-3 mobile MPC`、`mpcKeyId-only recovery`、`secure storage keyed by mpcKeyId`；`db_security_baseline_test.dart` 中有“Drift 不保存 MPC privateKey”断言；`rg -n "localEncryptedShare|challenge|rotationVersion" lib/data/remote/http.dart test/security/loggable_http_client_test.dart` 命中脱敏实现与测试；`flutter test test/db_security_baseline_test.dart test/mpc_signer_evm_flow_test.dart test/security/loggable_http_client_test.dart -r expanded` 与 `./tool/example_regression.sh` 全部通过。
    </acceptance_criteria>
    <verify>
      flutter analyze && flutter test test/db_security_baseline_test.dart test/mpc_signer_evm_flow_test.dart test/security/loggable_http_client_test.dart -r expanded && ./tool/example_regression.sh
    </verify>
    <done>
      Phase 6 的脱敏、安全边界、文档声明与主流程回归门禁都已跟上新的 MPC share 模型。
    </done>
  </task>

  <task id="06-02-03">
    <name>Task 3: Prove one real backend create/recover closure before phase sign-off</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      lib/service/address_service.dart
      lib/context/setup/wallet_setup_handler.dart
      test/address_service_injection_test.dart
      doc/integration.md
      doc/architecture/mpc_wallet_integration_plan.md
    </read_first>
    <files>
      test/address_service_injection_test.dart
      doc/integration.md
      doc/architecture/mpc_wallet_integration_plan.md
    </files>
    <action>
      在本阶段结束前，必须增加一条面向目标 backend 环境的真实闭环验证路径，而不只停在 fixture/fake backend。优先做法是在 `test/address_service_injection_test.dart` 增加一组 env-gated integration cases：仅当 `MPC_E2E_BASE_URL`、`MPC_E2E_PROJECT_ID`、`MPC_E2E_AUTH_TOKEN` 等必要环境变量存在时，执行一次真实 create 和一次真实 recover，断言 backend 返回完整 success payload，且 SDK 最终把地址、metadata 与 `localEncryptedShare` 正确写入各自边界。若目标 backend 当下无法满足该闭环，Phase 6 不能标记完成，必须显式拆 phase，而不是接受“暂不可用”作为终态。同步在文档中写明这条真实 backend 验证门禁和所需环境变量。
    </action>
    <acceptance_criteria>
      `rg -n "MPC_E2E_BASE_URL|MPC_E2E_PROJECT_ID|MPC_E2E_AUTH_TOKEN|real backend" test/address_service_injection_test.dart` 命中 env-gated real-backend 验证入口；`doc/integration.md` 或 `doc/architecture/mpc_wallet_integration_plan.md` 中明确写出 Phase 6 关闭前必须完成一次真实 create + recover；当测试环境变量可用时，`MPC_E2E_BASE_URL=... MPC_E2E_PROJECT_ID=... MPC_E2E_AUTH_TOKEN=... flutter test test/address_service_injection_test.dart -r expanded --plain-name "real backend"` 退出码为 0。
    </acceptance_criteria>
    <verify>
      MPC_E2E_BASE_URL=... MPC_E2E_PROJECT_ID=... MPC_E2E_AUTH_TOKEN=... flutter test test/address_service_injection_test.dart -r expanded --plain-name "real backend"
    </verify>
    <done>
      Phase 6 有真实 backend create/recover 闭环证据，不能再以“暂不可用”作为默认收尾。
    </done>
  </task>
</tasks>

## verification
- `flutter test test/address_service_injection_test.dart -r expanded`
- `flutter analyze && flutter test test/db_security_baseline_test.dart test/mpc_signer_evm_flow_test.dart test/security/loggable_http_client_test.dart -r expanded && ./tool/example_regression.sh`
- `MPC_E2E_BASE_URL=... MPC_E2E_PROJECT_ID=... MPC_E2E_AUTH_TOKEN=... flutter test test/address_service_injection_test.dart -r expanded --plain-name "real backend"`

## success_criteria
- create/recover 全面摆脱本地私钥/伪私钥
- recovery 契约按已拍板前提落地
- 至少一次 create + recover 成功闭环有自动化证据
- 至少一次面向目标 backend 的真实 create + recover 闭环有验证证据
- 日志脱敏与安全基线覆盖新增敏感字段
- 主流程回归门禁更新完成

## output
After execution, append orchestration, regression evidence, and backend contract notes to `.planning/phases/06-keygen-recovery/06-SUMMARY.md`.
