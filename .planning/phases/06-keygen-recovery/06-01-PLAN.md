---
phase: 06-keygen-recovery
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust/src/api.rs
  - rust/src/lib.rs
  - lib/data/remote/mpc/mpc_api.dart
  - lib/data/remote/mpc/mpc_models.dart
  - lib/data/remote/http.dart
  - lib/data/security/mpc_share_store.dart
  - lib/bridge/mpc/frb_generated.dart
  - lib/model/address_bean.dart
  - lib/service/mpc/signer.dart
  - test/address_bean_injection_test.dart
  - test/ceres_wallet_sdk_api_test.dart
  - test/mpc_share_store_test.dart
  - test/security/loggable_http_client_test.dart
autonomous: true
requirements:
  - TSS-02
must_haves:
  truths:
    - "客户端持有的 MPC local share 与完整私钥语义彻底分离。"
    - "MPC 创建/恢复后续编排可以在不落本地完整私钥的前提下继续推进。"
    - "宿主与 SDK 都不再需要用私钥占位去表达 MPC 账户。"
  artifacts:
    - path: rust/src/api.rs
      provides: "基于 `ZenGo-X/kms-secp256k1` 的 FRB-friendly Rust wrapper skeleton"
    - path: Cargo.toml
      provides: "Flutter <-> Rust bridge workspace/dependency entrypoint"
    - path: lib/data/security/mpc_share_store.dart
      provides: "按 mpcKeyId 保存/读取加密 local share 的 secure-storage repository"
    - path: lib/service/mpc/signer.dart
      provides: "EOA/MPC 分叉的 SignContext seam"
    - path: lib/model/address_bean.dart
      provides: "允许 MPC 地址不 materialize TWPrivateKey 的模型 seam"
  key_links:
    - from: rust/src/api.rs
      to: lib/bridge/mpc/frb_generated.dart
      via: "flutter_rust_bridge generated bindings expose Rust wrapper to Flutter"
      pattern: "frb\\(|keygen_start|recover_start|sign_start"
    - from: lib/data/remote/mpc/mpc_api.dart
      to: lib/data/remote/http.dart
      via: "MPC create/recover requests continue through the unified redacted HTTP client"
      pattern: "httpclient\\.post"
    - from: lib/model/address_bean.dart
      to: lib/service/mpc/signer.dart
      via: "MPC model seam no longer requires TWPrivateKey"
      pattern: "walletType == 'mpc'|SignContext"
    - from: lib/data/security/mpc_share_store.dart
      to: lib/data/remote/mpc/mpc_models.dart
      via: "mpcKeyId-keyed localEncryptedShare persistence"
      pattern: "mpc_share_v1:|localEncryptedShare"
---

# 06-01-PLAN

## objective
先建立真实 TSS create/recovery 能力所需的 contract/model/storage seam，避免后续实现继续复用 `privateKey` 语义。

## tasks
<tasks>
  <task id="06-01-01">
    <name>Task 1: Define MPC share storage and DTO boundaries</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      pubspec.yaml
      lib/data/remote/mpc/mpc_api.dart
      lib/data/remote/mpc/mpc_models.dart
      lib/data/security/secure_key_manager.dart
      lib/data/remote/http.dart
      test/security/loggable_http_client_test.dart
    </read_first>
    <files>
      Cargo.toml
      rust/src/api.rs
      rust/src/lib.rs
      lib/bridge/mpc/frb_generated.dart
      lib/data/remote/mpc/mpc_api.dart
      lib/data/remote/mpc/mpc_models.dart
      lib/data/remote/http.dart
      lib/data/security/mpc_share_store.dart
      test/mpc_share_store_test.dart
      test/security/loggable_http_client_test.dart
    </files>
    <action>
      先补执行前置设施：新增最小 Rust crate 与 `flutter_rust_bridge` wrapper skeleton，约定所有 Flutter 可见接口都通过 FRB-friendly DTO 暴露，而不是直接向 Dart 暴露 `kms-secp256k1` 的内部类型。wrapper 首批至少暴露 `keygen_start`、`keygen_continue`、`recover_start`、`recover_continue`、`sign_start`、`sign_continue` 这些空骨架或最小透传接口，后续 Phase 6/7 再填充真实协议。与此同时，新增 `lib/data/security/mpc_share_store.dart`，底层使用 `flutter_secure_storage`，存储键固定为 `mpc_share_v1:{mpcKeyId}`，只保存加密后的 live share blob。同步补齐 create/recover DTO：至少包含 `mpcKeyId`、`address`、`publicKey`、`curve`、`threshold`、`keyRef`、`backupState`、`rotationVersion`、`localEncryptedShare` 与 backup share envelope 对应字段。要求 `MpcApi` 的 create/recover 会话仍然复用统一 HTTP client，不允许新建裸 http client。同步修改 `lib/data/remote/http.dart` 与 `test/security/loggable_http_client_test.dart`，把 `localEncryptedShare`、challenge、可能新增的 recovery payload 字段纳入日志脱敏规则和自动化测试覆盖，确保这些敏感字段不会原样出现在日志中。
    </action>
    <acceptance_criteria>
      `rg -n "flutter_rust_bridge|kms-secp256k1" Cargo.toml rust/src/api.rs rust/src/lib.rs` 命中 bridge skeleton 与选定底座；`rg -n "keygen_start|recover_start|sign_start" rust/src/api.rs` 命中最小 wrapper 入口；`rg -n "mpc_share_v1:" lib/data/security/mpc_share_store.dart` 命中存储键规则；`rg -n "localEncryptedShare|rotationVersion|backupState" lib/data/remote/mpc/mpc_models.dart` 命中新 DTO 字段；`rg -n "httpclient\\.post" lib/data/remote/mpc/mpc_api.dart` 仍命中统一 HTTP client；`rg -n "localEncryptedShare|challenge|rotationVersion" lib/data/remote/http.dart test/security/loggable_http_client_test.dart` 命中新增脱敏实现与测试；`flutter test test/mpc_share_store_test.dart test/security/loggable_http_client_test.dart -r expanded` 退出码为 0。
    </acceptance_criteria>
    <verify>
      flutter test test/mpc_share_store_test.dart test/security/loggable_http_client_test.dart -r expanded
    </verify>
    <done>
      Rust wrapper / FRB skeleton、local share storage 和 DTO 边界都已具备，后续 create/recover 编排不再缺少客户端协议承载面。
    </done>
  </task>

  <task id="06-01-02">
    <name>Task 2: Split EOA and MPC model semantics</name>
    <read_first>
      .planning/phases/06-keygen-recovery/06-CONTEXT.md
      .planning/phases/06-keygen-recovery/06-RESEARCH.md
      lib/model/address_bean.dart
      lib/service/mpc/signer.dart
      lib/service/mpc/mpc_signer.dart
      test/address_bean_injection_test.dart
      test/ceres_wallet_sdk_api_test.dart
    </read_first>
    <files>
      lib/model/address_bean.dart
      lib/service/mpc/signer.dart
      test/address_bean_injection_test.dart
      test/ceres_wallet_sdk_api_test.dart
    </files>
    <action>
      调整 `AddressBean` 与 `SignContext` 语义，明确 `walletType == 'mpc'` 时不再强制 `TWPrivateKey`。`SignContext` 需要有清晰的 EOA/MPC 分叉构造或等价联合类型，MPC 分支最少持有 `mpcKeyId/address/accountId/chainId`。`AddressBean.fromAddress` 对 MPC 行禁止再调用 `TWPrivateKey.createWithHexString(decodedPrivateKey)`。同步修正相关测试，明确断言 MPC 分支允许 `privateKey == null` 且不会被当作 EOA。
    </action>
    <acceptance_criteria>
      `rg -n "walletType == 'mpc'|walletType != 'mpc'" lib/model/address_bean.dart lib/service/mpc/signer.dart` 命中显式语义分叉；`rg -n "TWPrivateKey\\.createWithHexString" lib/model/address_bean.dart` 不再命中 MPC 分支；`flutter test test/address_bean_injection_test.dart test/ceres_wallet_sdk_api_test.dart -r expanded` 退出码为 0。
    </acceptance_criteria>
    <verify>
      flutter test test/address_bean_injection_test.dart test/ceres_wallet_sdk_api_test.dart -r expanded
    </verify>
    <done>
      模型层已经能表达“客户端有 share，但没有本地完整私钥”的 MPC 状态，Phase 6/7 不会再被 `TWPrivateKey` 语义绑死。
    </done>
  </task>
</tasks>

## verification
- `flutter test test/mpc_share_store_test.dart -r expanded`
- `flutter test test/address_bean_injection_test.dart test/ceres_wallet_sdk_api_test.dart -r expanded`

## success_criteria
- local share 有独立 secure-storage 承载，不复用 `privateKey`
- DTO 已覆盖 create/recover 所需关键字段
- Address/Signer 模型对 MPC 分支不再要求 `TWPrivateKey`

## output
After execution, append model/storage seam notes to `.planning/phases/06-keygen-recovery/06-SUMMARY.md`.
