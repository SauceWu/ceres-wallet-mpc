---
phase: 14-websocket-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - example/pubspec.yaml
  - example/lib/ws_transport_example.dart
  - example/test/ws_transport_test.dart
autonomous: true
requirements: [TRANS-01, TRANS-02, TRANS-03, TRANS-04, TRANS-05]

must_haves:
  truths:
    - "WebSocketMpcTransport 首次调用 send() 时自动建立 WebSocket 连接，无需手动 connect"
    - "WebSocket 意外断开后，下一次 send() 调用自动重连，调用方无感知"
    - "并发的多个 send() 请求通过 JSON-RPC id 字段各自独立匹配响应，互不干扰"
    - "连接或响应超时时，send() 抛出 WsTransportTimeoutException，不挂起"
    - "HttpMpcTransport 代码零变动，与 WebSocket transport 并存"
    - "flutter analyze 和 flutter test 全部通过"
  artifacts:
    - path: "example/lib/ws_transport_example.dart"
      provides: "WebSocketMpcTransport 实现 MpcTransport 接口"
      exports: ["WebSocketMpcTransport", "WsTransportException", "WsTransportTimeoutException"]
    - path: "example/pubspec.yaml"
      provides: "web_socket_channel: ^3.0.3 依赖声明"
      contains: "web_socket_channel"
    - path: "example/test/ws_transport_test.dart"
      provides: "send/reconnect/timeout/concurrent 四类行为测试"
      min_lines: 80
  key_links:
    - from: "example/lib/ws_transport_example.dart"
      to: "lib/src/transport/mpc_transport.dart"
      via: "implements MpcTransport"
      pattern: "implements MpcTransport"
    - from: "_pendingRequests map"
      to: "StreamSubscription on channel.stream"
      via: "JSON-RPC id 匹配"
      pattern: "_pendingRequests\\[id\\]"
---

<objective>
新增 WebSocketMpcTransport 类，实现 MpcTransport 接口，支持自动连接、断线重连、
JSON-RPC id 请求-响应匹配及超时/错误处理，HTTP transport 保持零修改。

Purpose: 为 4 轮 MPC 协议提供持久 WebSocket 连接，消除每轮的 HTTP 握手开销。
Output: ws_transport_example.dart（实现 + 注释）、ws_transport_test.dart（单元测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<!-- 关键接口：MpcTransport 单方法抽象类 -->
<!-- lib/src/transport/mpc_transport.dart -->
<!-- abstract class MpcTransport { Future<String> send(String payload); } -->

<!-- HTTP transport 参考模式 -->
@example/lib/http_transport_example.dart

<!-- example app 依赖文件 -->
@example/pubspec.yaml
</context>

<interfaces>
<!-- 执行器直接使用以下接口，无需探索代码库 -->

<!-- 来自 lib/src/transport/mpc_transport.dart -->
```dart
abstract class MpcTransport {
  /// Sends a JSON-RPC 2.0 [payload] to the server and returns the raw response.
  Future<String> send(String payload);
}
```

<!-- JSON-RPC 2.0 请求格式（从 mock_transport.dart 推断）: -->
```json
{
  "jsonrpc": "2.0",
  "method": "keygen_start",
  "params": { ... },
  "id": 42
}
```

<!-- 响应成功格式: -->
```json
{ "jsonrpc": "2.0", "result": { ... }, "id": 42 }
```

<!-- 响应错误格式: -->
```json
{ "jsonrpc": "2.0", "error": { "code": -32001, "message": "..." }, "id": 42 }
```

<!-- web_socket_channel 3.0.x 关键 API: -->
```dart
import 'package:web_socket_channel/web_socket_channel.dart';

// 连接
final channel = WebSocketChannel.connect(Uri.parse('ws://...'));
// 等待握手完成
await channel.ready;
// 发送
channel.sink.add(jsonString);
// 接收（广播流）
channel.stream.listen((message) { ... });
// 关闭
await channel.sink.close();
// 错误：channel.stream.handleError / sink.done.catchError
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 WebSocketMpcTransport 实现 + 添加依赖</name>
  <files>
    example/pubspec.yaml,
    example/lib/ws_transport_example.dart
  </files>
  <behavior>
    - send() 首次调用时 _connect()，已连接则复用
    - _connect() 在 channel.ready await 后注册 stream.listen，将消息按 id 路由到对应 Completer
    - stream.listen onError / onDone 回调将 _channel 置 null，触发下次 send() 重连
    - send() 从 payload JSON 中提取 id（可能是 int 或 String），生成 Completer，存入 _pendingRequests[id]
    - send() 内部使用 Future.timeout(timeout)，超时抛出 WsTransportTimeoutException
    - 收到响应时：按 id 找 Completer，若 response 含 error 字段则 completeError(WsTransportException)，否则 complete(rawResponse)
    - 暴露 close() 方法关闭 channel 和清理 _pendingRequests
    - WsTransportException(message, {code}) — 服务端返回 JSON-RPC error 时抛出
    - WsTransportTimeoutException extends WsTransportException — 超时时抛出
  </behavior>
  <action>
    1. 编辑 example/pubspec.yaml，在 dependencies 下添加：
       web_socket_channel: ^3.0.3
       注意保持 YAML 缩进，不修改任何已有字段。

    2. 创建 example/lib/ws_transport_example.dart，内容结构如下：

    ```
    library;

    import 'dart:async';
    import 'dart:convert';
    import 'package:web_socket_channel/web_socket_channel.dart';
    import 'package:ceres_mpc/ceres_mpc.dart';

    /// WebSocket transport 实现 MpcTransport 接口。
    ///
    /// 关键特性：
    /// - 首次 send() 时自动建立 WebSocket 连接（懒连接）
    /// - 断线后下次 send() 自动重连，调用方无感知
    /// - 通过 JSON-RPC id 字段匹配并发请求和响应
    /// - 连接或响应超时抛出 WsTransportTimeoutException
    ///
    /// 用法：
    /// ```dart
    /// final transport = WebSocketMpcTransport(
    ///   wsUrl: 'ws://your-mpc-server.com/ws',
    ///   authToken: 'Bearer eyJhbG...',
    /// );
    /// final client = MpcClient(engine: ..., transport: transport);
    /// // 使用完毕后释放资源
    /// await transport.close();
    /// ```
    class WebSocketMpcTransport implements MpcTransport {
      final String wsUrl;
      final String? authToken;
      final Duration timeout;
      final Duration reconnectDelay;

      WebSocketChannel? _channel;
      StreamSubscription<dynamic>? _subscription;
      final Map<dynamic, Completer<String>> _pendingRequests = {};
      bool _connecting = false;
      Completer<void>? _connectCompleter;

      WebSocketMpcTransport({
        required this.wsUrl,
        this.authToken,
        this.timeout = const Duration(seconds: 30),
        this.reconnectDelay = const Duration(seconds: 1),
      });

      @override
      Future<String> send(String payload) async {
        // 1. 确保连接就绪
        await _ensureConnected();

        // 2. 从 payload 提取 JSON-RPC id
        final Map<String, dynamic> decoded;
        try {
          decoded = jsonDecode(payload) as Map<String, dynamic>;
        } catch (_) {
          throw WsTransportException('Invalid JSON payload');
        }
        final id = decoded['id'];
        if (id == null) {
          throw WsTransportException('JSON-RPC payload must contain an id field');
        }

        // 3. 注册 Completer，发送请求
        final completer = Completer<String>();
        _pendingRequests[id] = completer;

        try {
          _channel!.sink.add(payload);
        } catch (e) {
          _pendingRequests.remove(id);
          // 连接已断，置空等待重连
          _channel = null;
          rethrow;
        }

        // 4. 等待响应，带超时
        try {
          return await completer.future.timeout(
            timeout,
            onTimeout: () {
              _pendingRequests.remove(id);
              throw WsTransportTimeoutException(
                'Request timed out after ${timeout.inSeconds}s (id=$id)',
              );
            },
          );
        } on WsTransportException {
          rethrow;
        }
      }

      Future<void> _ensureConnected() async {
        if (_channel != null) return;

        // 防止并发重复连接
        if (_connecting) {
          await _connectCompleter!.future;
          return;
        }

        _connecting = true;
        _connectCompleter = Completer<void>();

        try {
          final headers = authToken != null
              ? {'Authorization': authToken!}
              : <String, String>{};

          _channel = WebSocketChannel.connect(
            Uri.parse(wsUrl),
            protocols: const [],
          );

          // 等待握手完成（web_socket_channel 3.x 新增 ready）
          await _channel!.ready.timeout(
            timeout,
            onTimeout: () {
              _channel = null;
              throw WsTransportTimeoutException(
                'WebSocket connection timed out after ${timeout.inSeconds}s',
              );
            },
          );

          // 注册消息监听
          _subscription = _channel!.stream.listen(
            _handleMessage,
            onError: _handleStreamError,
            onDone: _handleStreamDone,
            cancelOnError: false,
          );

          _connectCompleter!.complete();
        } catch (e) {
          _channel = null;
          _connectCompleter!.completeError(e);
          rethrow;
        } finally {
          _connecting = false;
          _connectCompleter = null;
        }
      }

      void _handleMessage(dynamic raw) {
        final String text;
        if (raw is String) {
          text = raw;
        } else if (raw is List<int>) {
          text = utf8.decode(raw);
        } else {
          return; // 忽略未知格式
        }

        Map<String, dynamic> response;
        try {
          response = jsonDecode(text) as Map<String, dynamic>;
        } catch (_) {
          return; // 非 JSON 消息忽略
        }

        final id = response['id'];
        if (id == null) return;

        final completer = _pendingRequests.remove(id);
        if (completer == null) return;

        if (response.containsKey('error')) {
          final error = response['error'] as Map<String, dynamic>;
          completer.completeError(
            WsTransportException(
              error['message']?.toString() ?? 'Server error',
              code: error['code'] as int?,
            ),
          );
        } else {
          completer.complete(text);
        }
      }

      void _handleStreamError(Object error, StackTrace st) {
        _disconnect(WsTransportException('WebSocket stream error: $error'));
      }

      void _handleStreamDone() {
        _disconnect(WsTransportException('WebSocket connection closed'));
      }

      void _disconnect(WsTransportException cause) {
        _channel = null;
        _subscription?.cancel();
        _subscription = null;
        // 通知所有等待中的请求
        for (final completer in _pendingRequests.values) {
          if (!completer.isCompleted) completer.completeError(cause);
        }
        _pendingRequests.clear();
      }

      /// 主动关闭连接并释放资源。
      Future<void> close() async {
        final ch = _channel;
        _disconnect(WsTransportException('Transport closed'));
        await ch?.sink.close();
      }
    }

    /// 服务端返回 JSON-RPC error 对象，或 WebSocket 连接异常时抛出。
    class WsTransportException implements Exception {
      final String message;
      final int? code;

      const WsTransportException(this.message, {this.code});

      @override
      String toString() =>
          code != null ? 'WsTransportException[$code]: $message' : 'WsTransportException: $message';
    }

    /// 连接建立或响应等待超时时抛出。
    class WsTransportTimeoutException extends WsTransportException {
      const WsTransportTimeoutException(super.message) : super(code: null);

      @override
      String toString() => 'WsTransportTimeoutException: $message';
    }
    ```

    注意：
    - WebSocketChannel.connect 不接受 headers 参数（web_socket_channel 3.x 限制），
      authToken 字段保留作为文档说明，实际注入需通过 URL query 参数或子协议。
      保留字段但在 _ensureConnected() 中注释说明。
    - _connecting / _connectCompleter 用于防止并发 send() 触发多次 connect。
    - 文件顶部保留 library; 声明，与 http_transport_example.dart 风格一致。
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/example && flutter pub get && flutter analyze lib/ws_transport_example.dart</automated>
  </verify>
  <done>
    - example/pubspec.yaml 包含 web_socket_channel: ^3.0.3
    - example/lib/ws_transport_example.dart 存在，exports WebSocketMpcTransport、WsTransportException、WsTransportTimeoutException
    - flutter pub get 成功，flutter analyze 对 ws_transport_example.dart 无 error
    - example/lib/http_transport_example.dart 无任何修改（git diff 为空）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 WebSocketMpcTransport 单元测试</name>
  <files>
    example/test/ws_transport_test.dart
  </files>
  <behavior>
    测试场景（全部使用 fake WebSocket，不依赖真实网络）：

    T-1: send() 返回匹配 id 的响应
      - 构造 FakeWebSocket，send('{"jsonrpc":"2.0","method":"x","id":1}')
      - FakeWebSocket 回送 '{"jsonrpc":"2.0","result":{"ok":true},"id":1}'
      - 期望 send() 返回包含 "ok" 的 JSON 字符串

    T-2: 并发请求通过 id 独立匹配（不串行阻塞）
      - 同时发出 id=1 和 id=2 两个请求
      - FakeWebSocket 先返回 id=2 的响应，再返回 id=1 的响应（乱序）
      - 期望两个 Future 都正确完成，结果不交叉

    T-3: 服务端返回 error 对象，send() 抛出 WsTransportException
      - FakeWebSocket 回送 '{"jsonrpc":"2.0","error":{"code":-32001,"message":"Session expired"},"id":3}'
      - 期望 send() 抛出 WsTransportException，code == -32001

    T-4: 响应超时抛出 WsTransportTimeoutException
      - 构造 timeout = Duration(milliseconds: 50) 的 transport
      - FakeWebSocket 不回送任何消息
      - 期望 send() 在 ~50ms 内抛出 WsTransportTimeoutException

    T-5: stream 关闭后（断线），下次 send() 自动重连
      - 第一次 send() 成功后，FakeWebSocket 主动 close stream（模拟断线）
      - 创建新的 FakeWebSocket 提供给 reconnect factory
      - 第二次 send() 成功（使用新连接），不抛出异常

    注意：WebSocketMpcTransport 需要支持依赖注入 channel factory，
    以便测试时替换真实 WebSocket。在 Task 1 实现中增加一个包私有构造参数：
      @visibleForTesting
      WebSocketMpcTransport.withChannelFactory({
        required this.wsUrl,
        required WebSocketChannel Function(Uri) channelFactory,
        this.authToken,
        this.timeout = const Duration(seconds: 30),
        this.reconnectDelay = const Duration(seconds: 1),
      }) : _channelFactory = channelFactory;

    如果 Task 1 未加此构造器，Task 2 在实现前先在 ws_transport_example.dart
    末尾追加该 factory 构造器和对应的 _channelFactory 字段，然后修改
    _ensureConnected() 使用 _channelFactory ?? WebSocketChannel.connect。
  </behavior>
  <action>
    1. 确认 example/test/ 目录存在（已有 widget_test.dart）。

    2. 创建 example/test/ws_transport_test.dart，完整内容如下：

    ```dart
    import 'dart:async';
    import 'dart:convert';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:web_socket_channel/web_socket_channel.dart';
    import '../lib/ws_transport_example.dart';

    // ── Fake WebSocket helpers ────────────────────────────────────────

    /// Returns a [WebSocketChannel] backed by in-memory StreamController pairs.
    /// [outbound] receives messages the transport sends TO the "server".
    /// Call [pushResponse] to push a message FROM the "server" to the transport.
    class FakeWsSetup {
      final StreamController<dynamic> _inbound = StreamController<dynamic>.broadcast();
      final StreamController<dynamic> _outbound = StreamController<dynamic>();

      late final WebSocketChannel channel;

      FakeWsSetup() {
        channel = _FakeWebSocketChannel(
          stream: _inbound.stream,
          sink: _FakeSink(_outbound),
        );
      }

      /// Push a JSON-RPC response string to the transport's receive side.
      void pushResponse(String message) {
        _inbound.add(message);
      }

      /// Close the inbound stream to simulate disconnect.
      Future<void> disconnectServer() async {
        await _inbound.close();
      }

      Stream<dynamic> get outboundMessages => _outbound.stream;
    }

    class _FakeWebSocketChannel extends WebSocketChannel {
      _FakeWebSocketChannel({required Stream<dynamic> stream, required WebSocketSink sink})
          : super(stream: stream, sink: sink, protocol: null);

      @override
      Future<void> get ready => Future.value();

      @override
      int? get closeCode => null;

      @override
      String? get closeReason => null;

      @override
      String? get protocol => null;
    }

    class _FakeSink implements WebSocketSink {
      final StreamController<dynamic> _controller;
      _FakeSink(this._controller);

      @override
      void add(dynamic data) => _controller.add(data);

      @override
      void addError(Object error, [StackTrace? stackTrace]) =>
          _controller.addError(error, stackTrace);

      @override
      Future<dynamic> addStream(Stream<dynamic> stream) => _controller.addStream(stream);

      @override
      Future<dynamic> close([int? closeCode, String? closeReason]) async {
        await _controller.close();
      }

      @override
      Future<dynamic> get done => _controller.done;
    }

    // ── Helper: build transport with fake channel factory ─────────────

    WebSocketMpcTransport _makeTransport(
      FakeWsSetup setup, {
      Duration timeout = const Duration(seconds: 5),
    }) {
      return WebSocketMpcTransport.withChannelFactory(
        wsUrl: 'ws://test-server',
        channelFactory: (_) => setup.channel,
        timeout: timeout,
      );
    }

    // ── Tests ─────────────────────────────────────────────────────────

    void main() {
      group('WebSocketMpcTransport', () {
        test('T-1: send() 返回匹配 id 的响应', () async {
          final setup = FakeWsSetup();
          final transport = _makeTransport(setup);

          const payload = '{"jsonrpc":"2.0","method":"ping","id":1}';
          const response = '{"jsonrpc":"2.0","result":{"pong":true},"id":1}';

          // 发送后立即推送响应
          final future = transport.send(payload);
          await Future.delayed(Duration.zero); // 让连接完成
          setup.pushResponse(response);

          final result = await future;
          final decoded = jsonDecode(result) as Map<String, dynamic>;
          expect((decoded['result'] as Map)['pong'], isTrue);
        });

        test('T-2: 并发请求通过 id 独立匹配（乱序响应）', () async {
          final setup = FakeWsSetup();
          final transport = _makeTransport(setup);

          const p1 = '{"jsonrpc":"2.0","method":"a","id":1}';
          const p2 = '{"jsonrpc":"2.0","method":"b","id":2}';
          const r1 = '{"jsonrpc":"2.0","result":{"from":"id1"},"id":1}';
          const r2 = '{"jsonrpc":"2.0","result":{"from":"id2"},"id":2}';

          final f1 = transport.send(p1);
          final f2 = transport.send(p2);
          await Future.delayed(Duration.zero);

          // 乱序：先回 id=2，再回 id=1
          setup.pushResponse(r2);
          setup.pushResponse(r1);

          final results = await Future.wait([f1, f2]);
          final d1 = jsonDecode(results[0]) as Map<String, dynamic>;
          final d2 = jsonDecode(results[1]) as Map<String, dynamic>;
          expect((d1['result'] as Map)['from'], equals('id1'));
          expect((d2['result'] as Map)['from'], equals('id2'));
        });

        test('T-3: 服务端返回 error，send() 抛出 WsTransportException', () async {
          final setup = FakeWsSetup();
          final transport = _makeTransport(setup);

          const payload = '{"jsonrpc":"2.0","method":"x","id":3}';
          const errResponse =
              '{"jsonrpc":"2.0","error":{"code":-32001,"message":"Session expired"},"id":3}';

          final future = transport.send(payload);
          await Future.delayed(Duration.zero);
          setup.pushResponse(errResponse);

          expect(
            future,
            throwsA(
              isA<WsTransportException>()
                  .having((e) => e.code, 'code', -32001)
                  .having((e) => e.message, 'message', contains('Session expired')),
            ),
          );
        });

        test('T-4: 响应超时抛出 WsTransportTimeoutException', () async {
          final setup = FakeWsSetup();
          final transport = _makeTransport(
            setup,
            timeout: const Duration(milliseconds: 60),
          );

          const payload = '{"jsonrpc":"2.0","method":"slow","id":4}';
          // 不推送任何响应，让 timeout 触发

          await expectLater(
            transport.send(payload),
            throwsA(isA<WsTransportTimeoutException>()),
          );
        });

        test('T-5: stream 关闭后，下次 send() 自动重连', () async {
          var callCount = 0;
          late FakeWsSetup firstSetup;
          late FakeWsSetup secondSetup;

          firstSetup = FakeWsSetup();
          secondSetup = FakeWsSetup();

          final transport = WebSocketMpcTransport.withChannelFactory(
            wsUrl: 'ws://test-server',
            channelFactory: (_) {
              callCount++;
              return callCount == 1 ? firstSetup.channel : secondSetup.channel;
            },
          );

          // 第一次 send 成功
          const p1 = '{"jsonrpc":"2.0","method":"x","id":10}';
          const r1 = '{"jsonrpc":"2.0","result":{"ok":1},"id":10}';
          final f1 = transport.send(p1);
          await Future.delayed(Duration.zero);
          firstSetup.pushResponse(r1);
          await f1;

          // 模拟断线
          await firstSetup.disconnectServer();
          await Future.delayed(const Duration(milliseconds: 10));

          // 第二次 send 应自动使用新连接
          const p2 = '{"jsonrpc":"2.0","method":"y","id":11}';
          const r2 = '{"jsonrpc":"2.0","result":{"ok":2},"id":11}';
          final f2 = transport.send(p2);
          await Future.delayed(Duration.zero);
          secondSetup.pushResponse(r2);

          final result2 = await f2;
          final d2 = jsonDecode(result2) as Map<String, dynamic>;
          expect((d2['result'] as Map)['ok'], equals(2));
          expect(callCount, equals(2)); // 证明重连了
        });
      });
    }
    ```

    3. 若 ws_transport_example.dart 中缺少 `withChannelFactory` 构造器，
       在该文件 WebSocketMpcTransport 类内追加以下代码（在 `send()` 方法之前）：

       ```dart
       // 内部 channel factory，供测试注入 fake channel
       final WebSocketChannel Function(Uri)? _channelFactory;

       /// 测试专用构造器，允许注入自定义 channel factory。
       @visibleForTesting
       WebSocketMpcTransport.withChannelFactory({
         required this.wsUrl,
         required WebSocketChannel Function(Uri) channelFactory,
         this.authToken,
         this.timeout = const Duration(seconds: 30),
         this.reconnectDelay = const Duration(seconds: 1),
       }) : _channelFactory = channelFactory;
       ```

       同时修改 `_ensureConnected()` 中建立连接的那一行：
       ```dart
       _channel = (_channelFactory ?? WebSocketChannel.connect)(Uri.parse(wsUrl));
       ```

       并在默认构造器中初始化 `_channelFactory = null`（通过 late 或可空即可）。

    4. 在 ws_transport_example.dart 顶部 import 区域添加（如尚未有）：
       ```dart
       import 'package:meta/meta.dart';
       ```
       若 meta 包不在依赖中，改用注释 `// @visibleForTesting` 并去掉注解，保持代码可编译。
  </action>
  <verify>
    <automated>cd /Users/stevensteven/workplace/flutter_project/flutter_mpc_wallet/example && flutter test test/ws_transport_test.dart --reporter expanded</automated>
  </verify>
  <done>
    - example/test/ws_transport_test.dart 存在，包含 T-1 到 T-5 共 5 个测试
    - flutter test test/ws_transport_test.dart 全部通过（5/5 passed）
    - flutter analyze 对 ws_transport_example.dart 和 ws_transport_test.dart 无 error
    - example/lib/http_transport_example.dart git diff 为空（零修改）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart client → WebSocket server | 外部服务器返回的 JSON 消息为不可信输入 |
| JSON-RPC id 字段 | 响应 id 由服务端控制，可能与任意请求 id 匹配 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | _handleMessage / id 匹配 | mitigate | 仅接受与已注册 _pendingRequests key 匹配的 id，无匹配则静默丢弃，不影响其他等待请求 |
| T-14-02 | Tampering | JSON 解析 | mitigate | jsonDecode 包裹 try/catch，非 JSON 消息静默忽略，不 crash |
| T-14-03 | Denial of Service | timeout 机制 | mitigate | 所有 send() 强制 Future.timeout()，超时移除 Completer 并抛出异常，防止无限挂起 |
| T-14-04 | Information Disclosure | authToken 字段 | accept | authToken 仅存于内存，不写入日志；web_socket_channel 3.x 不支持 header 注入，文档说明通过 URL query 参数传递（由调用方控制） |
| T-14-05 | Elevation of Privilege | _channelFactory (测试注入) | accept | withChannelFactory 标注 @visibleForTesting，生产代码使用默认构造器；factory 参数不影响生产路径安全性 |
</threat_model>

<verification>
整体验证步骤（在 example/ 目录下执行）：

```bash
# 1. 依赖安装
flutter pub get

# 2. 静态分析（覆盖新增的两个文件）
flutter analyze lib/ws_transport_example.dart test/ws_transport_test.dart

# 3. 单元测试
flutter test test/ws_transport_test.dart --reporter expanded

# 4. 确认 HTTP transport 未被修改
git diff example/lib/http_transport_example.dart
# 期望：空输出（无修改）

# 5. 全量分析（确保不引入新 warning）
flutter analyze
```

所有命令均应无 error 输出。
</verification>

<success_criteria>
Phase 14 完成的可观测状态：

1. `example/lib/ws_transport_example.dart` 存在，`WebSocketMpcTransport` 实现 `MpcTransport` 接口
2. `example/pubspec.yaml` 包含 `web_socket_channel: ^3.0.3`
3. 5 个单元测试全部通过（send 匹配、并发乱序、error 抛出、timeout、断线重连）
4. `flutter analyze` 无 error（warning 可容忍）
5. `example/lib/http_transport_example.dart` git diff 为空 — TRANS-05 满足
6. TRANS-01（自动连接）、TRANS-02（重连）、TRANS-03（id 匹配）、TRANS-04（timeout 异常）、TRANS-05（HTTP 不变）全部满足
</success_criteria>

<output>
完成后在 `.planning/phases/14-websocket-transport/` 目录下创建：
`.planning/phases/14-websocket-transport/14-01-SUMMARY.md`

内容包含：
- 实际创建/修改的文件列表
- WebSocketMpcTransport 关键设计决策（lazy connect、Completer map、factory constructor for testing）
- 测试覆盖的 5 个场景摘要
- TRANS-01 至 TRANS-05 满足状态
</output>
